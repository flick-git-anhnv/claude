# LESSONS-LOG — Nhật ký Lessons Learned (Toàn cục)

> Mỗi lần tạo/sửa lesson PHẢI thêm 1 dòng vào bảng bên dưới.

---

## Changelog

| # | Tác giả | Ngày | Loại | Nội dung | File |
|:--|:--------|:-----|:----:|:---------|:-----|
| 1 | vietanh / iPGSv4 | 2026-06-24 | Tính năng | Avalonia Migration từ WinForms — Pitfall & Pattern | `avalonia/avalonia-migration-from-winforms.md` |
| 2 | vietanh / iPGSv4 | 2026-06-24 | Tính năng | Tích hợp SDK Milesight: Port, Channel, Reconnect | `camera-integration/milesight-sdk-integration.md` |
| 3 | vietanh / IParkingDetectApp | 2026-06-24 | Lỗi | WinForms Designer không render programmatic UI | `csharp-winforms/winforms-designer-programmatic-ui.md` |
| 4 | vietanh / iPGSv4 | 2026-06-24 | Lỗi | AXAML: dùng `using:` thay `clr-namespace:` cho Avalonia built-in | `avalonia/avalonia-xmlns-using-vs-clr-namespace.md` |
| 5 | vietanh / iPGSv4 | 2026-06-24 | Lỗi | AvaloniaProperty subscription phải đặt ngoài guard `_control != null` | `avalonia/avalonia-property-subscription-before-visual-tree.md` |
| 6 | vietanh / iPGSv4 | 2026-06-24 | Lỗi | KzTabControl không render items — dùng TabControl chuẩn hoặc ControlTheme | `avalonia/kztabcontrol-no-render-programmatic-items.md` |
| 7 | vietanh / iPGSv4 | 2026-06-24 | Gotcha | Xác định đúng component/màn hình trước khi sửa tính năng UI | `avalonia/avalonia-find-actual-screen-before-editing.md` |
| 8 | vietanh / iPGSv4 | 2026-06-25 | Lỗi | DataGrid render trắng — thiếu StyleInclude Fluent theme | `avalonia/avalonia-datagrid-missing-fluent-styles.md` |
| 9 | vietanh / iPGSv4 | 2026-06-25 | Gotcha | Linux compat: đổi TFM, loại bỏ System.Drawing, exclude .resx WinForms | `dotnet-general/dotnet-linux-compat-tfm-resx-systemdrawing.md` |
| 10 | vietanh / iPGSv4 | 2026-06-25 | Gotcha | OpenCvSharp4.Windows kéo WpfExtensions → NETSDK1082 linux-x64 | `dotnet-general/dotnet-linux-compat-tfm-resx-systemdrawing.md` |
| 11 | vietanh / iPGSv4 | 2026-06-25 | Gotcha | Project WinForms trong .sln gây NETSDK1082 WindowsForms linux-x64 | `dotnet-general/dotnet-linux-compat-tfm-resx-systemdrawing.md` |
| 12 | vietanh / iPGSv4 | 2026-06-25 | Gotcha | Build pitfall khi port WinForms→Avalonia (Toolkit, ComboBox, SkiaSharp…) | `avalonia/avalonia-winforms-port-gotchas.md` |
| 13 | dungnn / iPGSv4 CCU | 2026-06-27 | Lỗi | Path tìm đường bị đứt: dùng foot-on-edge thay nearest-vertex | `avalonia/avalonia-canvas-pathfinding-winforms-port.md` |
| 14 | dungnn / iPGSv4 CCU | 2026-06-26 | Lỗi | Avalonia TabControl — columns không hiển thị ở tab chưa mở lần đầu | `avalonia/avalonia-tabcontrol-lazy-visual-tree-columns.md` |
| 15 | kienpt / iPGSv4 CCU | 2026-06-26 | Lỗi | SQL injection qua legacy FillData() — không bắt khi build Windows | `database/sql-injection-legacy-filldata.md` |
| 16 | kienpt / iPGSv4 CCU | 2026-06-26 | Pattern | 18 issues điển hình bị bỏ sót khi migrate WinForms→Avalonia | `avalonia/avalonia-migration-review-checklist.md` |
| 17 | kienpt / iPGSv4 CCU | 2026-06-27 | Lỗi | ListBox PointerPressed không fire — dùng `AddHandler(handledEventsToo: true)` | `avalonia/avalonia-listbox-pointerpressed-handled.md` |
| 18 | vietanh / Claude-Git (code-migrator agent) | 2026-07-01 | Gotcha | code-migrator bỏ sót tính năng + thiếu dependency cross-platform lặp lại — thêm gate Cấp 0 (đếm khớp inventory) + G6 re-check dependency cuối + publish linux-x64 bắt buộc | `avalonia/avalonia-migration-review-checklist.md` |
| 19 | vietanh / parking-v8-app (ClearVehicleTool) | 2026-07-02 | Lỗi | Dùng IParkingv8.API từ project mới crash NullReferenceException nếu quên init SystemUtils.logger | `dotnet-general/iparkingv8-api-systemutils-logger-null.md` |
| 20 | vietanh / iPGSv4 (ccu-avalonia) | 2026-07-02 | Gotcha | BaseLIB ProjectReference broken (62 errors) → switch sang `<Reference><HintPath>` pre-built DLL; System.Drawing.Common Windows-only; cascade check bắt buộc sau `<Compile Remove>` | `dotnet-general/dotnet-baselib-broken-projectref-prebuilt-dll.md` |
| 21 | vietanh / KZTEK Image Tools (d:\Tool) | 2026-07-02 | Tính năng | YOLO Detect thêm nhận dạng liên tục từ link YouTube — yt-dlp resolve stream URL | `camera-integration/yt-dlp-youtube-stream-cv2.md` |
| 22 | vietanh / KZTEK Image Tools (d:\Tool) | 2026-07-02 | Tính năng | YouTube detect: bổ sung thanh tua (seek) cho VOD + chọn độ phân giải (Best/1080p.../Worst) | `camera-integration/yt-dlp-youtube-stream-cv2.md` |
| 23 | vietanh / KZTEK Image Tools (d:\Tool) | 2026-07-02 | Gotcha | Link aggregator camera công cộng (opencctv.org) không mở được bằng cv2 — phải tìm "Feed URL"/DevTools để lấy link gốc (vd wzmedia.dot.ca.gov m3u8); RTSP/HTTP field có sẵn dùng ngay, không cần sửa code | `camera-integration/public-traffic-cam-find-direct-stream-url.md` |
| 24 | vietanh / 3rdLedLib (KZtek.ControllerLib) | 2026-07-04 | Gotcha | File `SocketHelpers/UdpTools.cs` đã có sẵn trong project đích vẫn giữ `namespace Kztek.Controller.SocketHelpers` (copy nguyên từ project cũ), không khớp đường dẫn thư mục — build CS0234 khi port class mới đoán namespace theo path | `networking-protocol/port-legacy-namespace-mismatch.md` |
| 25 | vietanh / 3rdLedLib (KZtek.ControllerLib.Framework) | 2026-07-06 | Gotcha | Port `EventHandler<DispenserStatus>` từ .NET 8 sang .NET Framework — CS0311 vì mscorlib có ràng buộc `where TEventArgs : EventArgs` (đã gỡ trên .NET Core/.NET 5+); fix: cho `DispenserStatus` kế thừa `EventArgs` | `dotnet-general/netframework-eventhandler-requires-eventargs-constraint.md` |
| 26 | dungnn / parking-v8-app (IParkingv8 — env-prerequisite-check) | 2026-07-08 | Gotcha | `KzButton` (Kztek.Control8) tự reset FillColor/BorderColor/ForeColor về mặc định mỗi khi Focus đổi (GotFocus/LostFocus nội bộ) — set màu custom 1 lần sẽ bị ghi đè; fix: subscribe GotFocus/LostFocus của mình SAU để áp lại màu | `csharp-winforms/kzbutton-focus-events-override-custom-colors.md` |
| 27 | kienpt / Kztek.Camera (G5.10 migrate CameraDescriptionPage) | 2026-07-13 | Gotcha | MsBox.Avalonia 3.0.0-rc2: `ButtonEnum` ở `MessageBox.Avalonia.Enums` (legacy ns), `Icon` ở `MsBox.Avalonia.Enums` (mới) — CS0103 nếu chỉ dùng `using MsBox.Avalonia.Enums` | `avalonia/msbox-avalonia-namespace-split.md` |
| 28 | kienpt / Kztek.Camera (G3.6+G5.1 wrap KzZoneEditor thay DefineRegionsControl) | 2026-07-14 | Pattern | Wrap Kz Avalonia component pre-built (không có source) khi thiếu API vs WinForms control gốc: probe API bằng MetadataLoadContext → phân loại A/B/C → 3-workaround W1 Grid+Image / W2 IsHitTestVisible / W3 scale trong VM. Tiết kiệm 80% effort so build mới control Avalonia thuần | `avalonia/kz-component-wrap-workaround-pattern.md` |
| 29 | kienpt / parking-v8-app-avalonia (Kztek.Cameras.Avalonia — audit cross-platform packages) | 2026-07-14 | Gotcha | ReadPL 1.0.8 (Multi-Platform) đổi API `Recognize()`/`Plate.Crop` sang thẳng `SkiaSharp.SKBitmap`, bỏ hẳn `System.Drawing.Bitmap`/`Image` như bản 1.0.3 — comment code cũ lỗi thời khiến tưởng cần thêm `System.Drawing.Common`; dùng reflection load DLL để xác nhận API thật, xóa 2 helper convert thừa | `camera-integration/readpl-api-changed-skbitmap-not-systemdrawing.md` |
| 30 | kienpt / iPGSv4 (IPGS.Object — IInvoiceService) | 2026-07-15 | Gotcha | Class library cũ không có `<ImplicitUsings>enable</ImplicitUsings>` trong csproj → `Task<>` CS0246 khi tạo interface async; fix: thêm `using System.Threading.Tasks;` vào file, KHÔNG sửa csproj library để tránh cascade conflict | `dotnet-general/dotnet-no-implicitusings-task-not-found.md` |
| 31 | dungnn / parking-v8-app-avalonia (ParkingV8.App — bật F12 DevTools) | 2026-07-15 | Gotcha | `Application.AttachDevTools()` gọi SAU khi đã gán `desktop.MainWindow` → window đầu tiên bị attach handler 2 lần (1 lần attach ngay vì MainWindow đã có, 1 lần nữa qua sự kiện `WindowOpened`) → bấm F12 một lần mở ra 2 cửa sổ Avalonia DevTools giống hệt nhau; fix: gọi `AttachDevTools()` TRƯỚC dòng gán `desktop.MainWindow = ...` | `avalonia/avalonia-attachdevtools-double-window.md` |
| 32 | vietanh / parking-v8-app-avalonia (ParkingV8.App — localization key refactor) | 2026-07-16 | Pattern | `AppLocalizationService.Get("key")` bằng magic string dễ gõ sai/không dịch được (im lặng fallback về key gốc) — port sang property mạnh kiểu như bản WinForms (`CurrentResources.FrmLogin`), JSON deserialize thẳng vào property qua reflection cache; phát hiện luôn 1 key JSON lỗi thật `"FrmSelectLane: "` lọt qua nhiều lần build/deploy vì key chỉ là string tự do | `ui-patterns/avalonia-localization-typed-resource-keys.md` |
| 33 | vietanh / parking-v8-app-avalonia (ParkingV8.UI — CameraView fix AccessViolationException) | 2026-07-16 | Lỗi | **Sửa lại lesson #29 (SAI):** `GetCurrentVideoFrameAsync()` trả tham chiếu SỐNG (`_currentSKFrame` trong `AnvPlayer`), KHÔNG phải bản clone — dispose nó (như lesson #29 khuyên) làm native memory bị free trong khi vẫn đang render/return vào `SKBitmapPool`, gây `System.AccessViolationException` crash tại `SKBitmapPool.Return()`; fix: bỏ `.Dispose()` trong `CaptureSnapshotBitmapAsync()` + `ProbeFirstFrameAsync()` | `camera-integration/kztek-cameras-avalonia-getframe-returns-skbitmap.md` |
| 34 | kienpt / parking-v8-app-avalonia (ParkingV8.UI — KzPasswordTextBox border không hiển thị) | 2026-07-16 | Lỗi | `Border.ClearValue(BorderBrushProperty)` trong code-behind (`ApplyInputState()` nhánh default + `ApplyBorderDefault()`) xóa luôn binding XAML gốc `{Binding BorderBrush, RelativeSource=AncestorType=UserControl}` — border biến mất ở trạng thái Normal (chỉ Error/Disabled còn border vì set trực tiếp); fix: set brush tường minh qua `ResolveBrush("Brush.InputBorder", ...)` thay vì ClearValue | `avalonia/avalonia-clearvalue-removes-xaml-binding.md` |
| 35 | trongtv / IPGS.Kiosk.Avalonia (STEP-2.B4 frmSelectVehicleType) | 2026-07-20 | Gotcha | Namespace `IPGS.Kiosk.Avalonia` che khuất `Avalonia.*` framework → CS0234 khi viết `Avalonia.VisualTreeAttachmentEventArgs`; fix: `using Avalonia;` + unqualified type HOẶC `global::Avalonia.Type` | `avalonia/avalonia-namespace-collision-ipgs-kiosk.md` |
| 36 | ANHNV_2025 / iPGSv4 (KioskDeployTool.ps1 — remote deploy GUI) | 2026-07-23 | Lỗi | File `.ps1` UTF-8 KHÔNG BOM chứa ký tự Unicode (em dash, tiếng Việt) khiến Windows PowerShell 5.1 đọc nhầm ANSI codepage → string bị cắt giữa chừng, lỗi parser xuất hiện ở các dòng XA không liên quan; fix `Set-Content -Encoding UTF8` (PS 5.1 tự thêm BOM) | `dotnet-general/powershell-utf8-no-bom-string-parse-corruption.md` |
| 37 | ANHNV_2025 / KZTEK Image Tools (d:\Tool — audit hiệu năng) | 2026-07-26 | Gotcha | App Tkinter khởi động 46s vì import ML ở mức module — thủ phạm nặng nhất là `paddleocr` trong `core/imports.py` kéo theo torch+transformers GIÁN TIẾP (grep `import torch` không ra); truy vết bằng hook `builtins.__import__`; fix `find_spec()` cho cờ `_X_OK` + proxy lazy cho object + dựng tab lười → 2,1s | `python-tkinter/python-lazy-import-startup-time.md` |

---

## Ghi chú nhanh (Key findings)

| # | Key finding |
|:--|:-----------|
| 1 | Thread → `Dispatcher.UIThread.Post`; Binding → INPC; Navigation → ContentControl + ViewModel routing |
| 2 | HTTP port 80 ≠ RTSP port 554; Channel bắt đầu từ 1; dùng `int` (không phải `string`) cho port/channel |
| 3 | Đổi `*.Designer.cs` → `*.UI.cs`; thêm `[DesignerCategory("Code")]` |
| 4 | `xmlns:conv="using:Avalonia.Data.Converters"` — `clr-namespace:;assembly=Avalonia` không hoạt động với XamlX |
| 5 | `OnPropertyChanged` chạy TRƯỚC `OnAttachedToVisualTree`; bọc subscription trong `if (_grid != null)` → bị skip |
| 6 | Dùng `<TabControl>` chuẩn; hoặc thêm `ControlTheme BasedOn FluentTheme TabControl` |
| 7 | `KzZoneEditor` không được dùng trong app; file thực tế là `CameraSettingWindow.axaml.cs` — grep trước khi sửa |
| 8 | Thêm `<StyleInclude Source="avares://Avalonia.Controls.DataGrid/Themes/Fluent.xaml" />` vào `App.axaml` |
| 9 | `<Compile Remove>` không tự exclude `.resx` cùng tên; phải thêm `<EmbeddedResource Remove>` |
| 10 | Thay `OpenCvSharp4.Windows` → `OpenCvSharp4` + `OpenCvSharp4.runtime.win` |
| 11 | Project `net8.0-windows+UseWindowsForms` trong .sln gây fail build linux-x64 — xóa ra khỏi .sln |
| 12 | `[ObservableProperty]` trùng tên method → CS0102; `Theme` đụng `Control.Theme` → dùng alias `Brand` |
| 13 | `NearestEdgeIdx+FootOnEdge` thay `GetNearestVertex`; `BuildRoadPoints` 4 combo BFS; 1 polyline liên tục |
| 14 | `OnAttachedToVisualTree` chưa chạy → `InnerGrid=null`; dùng `_pendingColumns` để defer column setup |
| 15 | Mọi string user/DB vào raw SQL PHẢI `.Replace("'", "''")` — compiler không warn |
| 16 | Security, System.Drawing, race condition, MVVM anti-pattern, CODE-GRAPH sync — luôn build linux-x64 |
| 17 | ListBoxItem mark PointerPressed handled → `+=` bị skip; fix: `AddHandler(PointerPressedEvent, ..., handledEventsToo: true)` |
| 18 | code-migrator PHẢI in số liệu `Tổng file nguồn: N \| Tổng control/timer/event: M` khớp inventory; PHẢI re-check dependency + publish linux-x64 ở G6 (cuối), không chỉ ở G1 (đầu) |
| 19 | `SystemUtils.logger` static, không init sẵn — PHẢI gọi `SystemUtils.logger = LoggerFactory.CreateLoggerService(EmLogServiceType.OFFLINE_FILE, path)` trước khi dùng bất kỳ class nào trong IParkingv8.API |
| 20 | BaseLIB broken → dùng `<Reference><HintPath> ...bin\x64\Release\netstandard2.1\Kztek.Tool.dll</HintPath>` thay `ProjectReference`; `netstandard2.1` DLL dùng được cho cả win-x64 lẫn linux-x64; sau `<Compile Remove Foo.cs>` PHẢI grep `new Foo(` trong toàn project còn compile |
| 21 | `yt_dlp.YoutubeDL(...).extract_info(url, download=False)` → `info["url"]` là direct stream URL, `cv2.VideoCapture` mở trực tiếp; luôn resolve trong thread nền (mất 1-3s network) tránh treo UI Tkinter |
| 22 | Aggregator camera công cộng (opencctv.org) hay có sẵn link "Feed URL" trong UI trang — copy thẳng, không cần dò Network tab; ưu tiên domain gốc (`wzmedia.dot.ca.gov`) hơn proxy riêng của aggregator (`/api/hls/...?s=token`) |
| 23 | `KzButton` GotFocus/LostFocus (private, không unsubscribe được) luôn reset FillColor/BorderColor/ForeColor về `ButtonColorManagement` mặc định — muốn màu custom cố định thì `btn.GotFocus += (s,e)=>btn.FillColor=custom; btn.LostFocus += (s,e)=>btn.FillColor=custom;` (subscribe SAU khi tạo control để chạy sau handler nội bộ) |
| 24 | Nghi ngờ API 3rd-party SDK (comment cũ nói ngược lại thực tế) → `Assembly.LoadFrom(dll)` + `GetTypes()/GetMethods()/GetProperties()` liệt kê signature thật, nhanh hơn tin comment hoặc decompiler; so `.nuspec` 2 version trong `~/.nuget/packages/<pkg>/<v>/` để thấy dependency graph đổi (System.Drawing.Common → SkiaSharp) là dấu hiệu API đổi theo |
| 25 | Avalonia `Dispatcher.UIThread.InvokeAsync()` trả `DispatcherOperation` — không có `.AsTask()` (WPF pattern). Dùng `async Task` + `await Dispatcher.UIThread.InvokeAsync(...)` trực tiếp. | `avalonia/avalonia-dispatcher-invokeAsync-no-AsTask.md` |
| 26 | Class library .NET 8 cũ (IPGS.Object) không có `<ImplicitUsings>enable</ImplicitUsings>` → `Task<>` không tìm thấy (CS0246); thêm `using System.Threading.Tasks;` vào file, KHÔNG thêm ImplicitUsings vào csproj library. | `dotnet-general/dotnet-no-implicitusings-task-not-found.md` |
| 27 | Nâng Avalonia 11→12 xóa hẳn `GotFocusEventArgs` + `ExtendClientAreaChromeHints`, siết AVLN2100 (bắt buộc `x:DataType` trong DataTemplate/Popup/Flyout). Nâng RabbitMQ.Client 6→7 rewrite async-only (`IModel`→`IChannel`, `*Async` suffix, `IAsyncDisposable`) — bridge sync/async 1 điểm bằng `ConfigureAwait(false)+GetAwaiter().GetResult()`; `lock` không await được → đổi `SemaphoreSlim`. `Avalonia.Diagnostics` (cũ) ngừng ở 11.3.18 → Avalonia 12 dùng gói mới `AvaloniaUI.DiagnosticsSupport`, API đổi `AttachDevTools()`→`AttachDeveloperTools()`, gọi trong `Initialize()` (không phải `OnFrameworkInitializationCompleted()`). | `avalonia/avalonia-12-breaking-changes-rabbitmq7-migration.md` |
| 28 | vietanh / parking-v8-app-avalonia | 2026-07-16 | Lỗi | Port loading flow WinForms→Avalonia rớt nhánh "computer chưa đăng ký" (không lỗi build nhưng sai hành vi, kẹt ở trạng thái Running mãi mãi, mở MainShell với data rỗng) | `avalonia/avalonia-migration-silent-behavior-drop-loading-flow.md` |
| 29 | vietanh / parking-v8-app-avalonia | 2026-07-16 | Gotcha | `Camera.GetCurrentVideoFrameAsync()` trong `Kztek.Cameras` (external Avalonia lib) trả `SKBitmap`, không phải `Avalonia.Bitmap` — cần convert qua `SkiaAvaloniaAdapter.ToAvaloniaBitmapJpeg()` + dispose thủ công | `camera-integration/kztek-cameras-avalonia-getframe-returns-skbitmap.md` |
| 30 | vietanh / Kztek.Camera | 2026-07-16 | Lỗi | `git push` bị GitHub từ chối vì `libavcodec.so` (100.85MB) > giới hạn 100MB — đã lỡ commit vào lịch sử, phải `git filter-branch` xóa khỏi toàn bộ lịch sử + xóa `refs/stash` (giữ tham chiếu tới blob cũ) rồi mới `gc --prune=now` sạch thật | `dotnet-general/github-push-rejected-file-over-100mb-native-binary.md` |
| 31 | vietanh / parking-v8-app-avalonia | 2026-07-16 23:50 | Lỗi | `camera.Type` (int) bị decode nhầm bằng SAI enum (`Kztek.Cameras.CameraType` thay vì decoder gốc `Camera.GetCameraType()`) — 3 enum "loại camera" khác thứ tự cùng tồn tại trong repo khiến camera Custom bị hiểu nhầm thành HANET, build sai RTSP URL, treo "Connecting" mãi không log lỗi; chỉ lỗi ở LaneSettingsWindow vì màn hình chính dùng lại chuỗi đã decode đúng sẵn thay vì tự decode lại từ int | `camera-integration/camera-type-int-decoded-with-wrong-enum.md` |
| 32 | vietanh / parking-v8-app-avalonia | 2026-07-17 00:05 | Gotcha | `KzNumericUpDown` (horizontal) có 3 phần Width cố định (45+80+45=170px), không co giãn — nhét vào cột Grid hẹp hơn 170px (sau khi thu gọn panel Vùng Loop) khiến control tràn ra ngoài border, không có exception/warning cảnh báo | `ui-patterns/kznumericupdown-fixed-width-column-overflow.md` |
| 33 | vietanh / parking-v8-app-avalonia | 2026-07-17 | Lỗi | `KzPasswordTextBox` nút mắt "bấm hiện mật khẩu không có tác dụng" — `OnAttachedToVisualTree` bị gọi lại nhiều lần, `btn.Click +=` cộng dồn handler khiến 1 lần bấm toggle chẵn lần, quay về trạng thái cũ; fix bằng cờ `_eventsWired` chống gắn event trùng | `avalonia/avalonia-onattachedtovisualtree-duplicate-event-wiring.md` |
| 34 | vietanh / parking-v8-app-avalonia | 2026-07-17 | Lỗi | TextBox bind path lồng `"Vm.Model.Field"` (Model non-INPC) không cập nhật sau khi code-behind set field rồi raise `PropertyChanged("Model")` — reference object cha không đổi nên Avalonia binding coi là no-op, không lan refresh xuống field con; fix bằng proxy property trên ViewModel bind trực tiếp | `avalonia/avalonia-nested-path-binding-no-refresh-same-reference.md` |
| 35 | vietanh / parking-v8-app-avalonia | 2026-07-17 | Lỗi | Camera live preview (`CameraView.axaml`) tràn ra ngoài border bo góc, video không lấp đầy khung — `Border` có `CornerRadius` không tự clip nội dung con (thiếu `ClipToBounds="True"`), Control video gán động vào `Border.Child` bằng code-behind không tự Stretch | `avalonia/avalonia-border-child-not-clipped-not-stretched.md` |
| 36 | kienpt / IPGSv1 | 2026-07-18 09:00 | Tính năng | Tích hợp bảng LED ngoài trời mới P5 (UDP string protocol) — hạ tầng `OutDoorLed`/`UdpTools`/`CmdBuilder` sẵn có đã generic theo `EM_ModuleType`, chỉ cần thêm `EM_ModuleType.P5` + case switch (resolution rule, color, FontSize) thay vì viết class mới; FontSize P5 (10/20/30) là thang giá trị khác hẳn point-size P10 dù trùng số 10 — phải lọc riêng theo module type ở `frmLedLineConfig` | `networking-protocol/kz-led-p5-udp-outdoor-integration.md` |
| 37 | kienpt / IPGSv1 | 2026-07-18 09:10 | Gotcha | `MSB3541: Files has invalid value "<<<<<<< HEAD"` khi build IPGSv1 — không nằm trong .csproj mà trong file cache `obj/Debug/*.csproj.FileListAbsolute.txt` sót conflict marker git từ lần merge cũ; xóa file cache (an toàn, tự sinh lại) là hết lỗi | `dotnet-general/msbuild-obj-filelistabsolute-stale-conflict-marker.md` |
| 38 | vietanh / parking-v8-app-avalonia (BUG-004 vòng 2) | 2026-07-18 | Lỗi | ⚠️ CRITICAL: `AttachedToVisualTree` fires trước Measure/Arrange → `Bounds.Width=0` → camera decode 1920×1080 thay vì ~300px; kết hợp Motion detection + multi-lane → CPU spike → "Not Responding". Fix: `DispatcherPriority.Loaded` + `motionDetectionInterval=100` + 4 SDK fixes | `avalonia/avalonia-attachedtovisualtree-zero-bounds-camera-motion.md` |
| 39 | vietanh / parking-v8-app-avalonia | 2026-07-18 09:55 | Lỗi | Build `.deb` qua WSL báo "A compatible .NET SDK was not found, Requested SDK version: 9.0.313" dù đã cài `dotnet-install.sh --channel 8.0` thành công — `global.json` pin SDK version (9.0.313) độc lập với `TargetFramework` (net8.0) trong .csproj; phải đọc `global.json` để biết channel thật cần cài, không suy luận từ TargetFramework | `dotnet-general/wsl-linux-deb-build-global-json-sdk-version-mismatch.md` |
| 40 | vietanh / iPGSv4 | 2026-07-20 | Lỗi | Đổi `ProjectReference` `IPGS.Object` sang bộ `Kztek.*.MultyPlatform` mới làm rớt hàng loạt type transitive không liên quan trực tiếp (RestSharp.Authenticators.Digest, ILpr từ iParkingv5.Lpr, System.Drawing.Common ở Kztek.Object.Entity) gây build fail dây chuyền qua nhiều project; build lặp `--no-incremental` từng project một để lộ hết lỗi thật | `dotnet-general/projectreference-swap-drops-transitive-deps-silently.md` |
| 41 | vietanh / iPGSv4 | 2026-07-20 | Lỗi | Cập nhật lại lesson Avalonia 12: `GotFocusEventArgs` → `Avalonia.Input.FocusChangedEventArgs` (KHÔNG phải RoutedEventArgs như ghi trước đó); phát hiện thêm `AncestorType=UserControl` không resolve property con (dùng `x:Name`+`ElementName`) và `TreeViewItem.ShowExpander` bị xóa hẳn | `avalonia/avalonia-12-breaking-changes-rabbitmq7-migration.md` |
| 42 | vietanh / iPGSv4 | 2026-07-20 | Tính năng | Đổi engine LPR `iParkingv5.Lpr` (Windows-only, thiếu HawkeyeCS.dll) → `ParkingV8.Lpr` (cross-platform, ReadPL+SkiaSharp) theo yêu cầu user giữa chừng — `ILpr` mới nhận thẳng SKBitmap nên bỏ hẳn bridge System.Drawing đã viết trước đó, chỉ còn `GetPlateNumberAsync`, `LprFactory` còn 1 tham số, `LoggerFactory` đổi namespace con `Kztek.Tool.LogHelpers` | `camera-integration/lpr-engine-swap-iparkingv5-to-parkingv8-skiasharp.md` |
| 43 | vietanh / iPGSv4 (dialog Sửa Camera) | 2026-07-20 | Lỗi | `KzButton` Thickness Padding "H,V" áp H cho CẢ 2 bên (không chia đôi) — nút +/- `KzNumericUpDown` Width=40 + kz-size-md padding 16,0 = 32px ngang nuốt gần hết glyph, chỉ còn thấy dấu chấm; cùng gốc rễ gây cột DataGrid (Port/Kênh/ZCU index) hẹp bị cắt chữ. Fix: `Padding="0"` local trên nút icon-only + giảm padding token DataGridCell/Header | `avalonia/avalonia-kzbutton-padding-swallows-narrow-glyph.md` |
| 44 | vietanh / iPGSv4 (IPGSUseCam LiveView) | 2026-07-20 | Gotcha | Thêm project `Kztek.Cameras.Avalonia` vào solution + wiring LiveView: map `IPGS.Object` CameraType (HIKVision/Dahua/Milesight) sang `Kztek.Cameras.CameraType` — HIKVision phải map `HIKVISION2` (bản `HIKVISION` không dấu số không có builder RTSP), Milesight KHÔNG có builder nào trong `CameraRtspUrlBuilderFactory`, phải fallback `Custom` + tự build URL RTSP thủ công (giả định path `/main`, chưa xác nhận thiết bị thật) | `camera-integration/ipgs-camera-type-milesight-no-rtsp-builder.md` |
| 45 | vietanh / iPGSv4 (IPGSUseCam LiveView) | 2026-07-20 | Tính năng | User cung cấp tài liệu RTSP Milesight chính hãng xác nhận scheme `rtsp://ip:port/main|sub|third` (port mặc định 554) — thêm chính thức `CameraType.Milesight` (cuối enum, không dịch ordinal) + `BuildMilesight()` trong `CameraRtspUrlBuilderFactory.cs` (chọn path theo `StreamIndex`), bỏ hack `Custom` tạm thời ở `MainWindow.axaml.cs` | `camera-integration/ipgs-camera-type-milesight-no-rtsp-builder.md` |
| 46 | vietanh / iPGSv4 (IPGSUseCam LiveView) | 2026-07-20 | Lỗi | `BuildMilesight()` bản đầu chọn stream theo `Camera.StreamIndex` nhưng `MainWindow.axaml.cs` chỉ set `Chanel`, không bao giờ set `StreamIndex` → luôn đọc default (im lặng, luôn mở nhầm "/sub"); fix đổi sang dùng `Camera.Chanel`, đánh số kênh bắt đầu từ 1 (parity Dahua/HIKVISION2/CNB: `Chanel<=0?1:Chanel`) | `camera-integration/ipgs-camera-type-milesight-no-rtsp-builder.md` |
| 45 | dungnn / iPGSv4 (SqlClient → Ubuntu) | 2026-07-20 | Lỗi | Đổi `System.Data.SqlClient` → `Microsoft.Data.SqlClient` để hỗ trợ Linux — build pass nhưng connect thật lỗi "SSL certificate chain was issued by an authority that is not trusted" vì `Microsoft.Data.SqlClient` mặc định validate cert chain nghiêm ngặt hơn, SQL Server on-prem dùng self-signed cert bị từ chối login | `database/sqlclient-migration-ssl-cert-trust-error.md` |
| 47 | kienpt / iPGSv4 (dialog Sửa Camera) | 2026-07-20 | Lỗi | Sửa camera đang chạy (Channel/IP/Port/User/Pass) qua `CameraWindow` — DB + `CameraObject` in-memory update đúng, nhưng camera thực tế vẫn hoạt động theo giá trị CŨ vì `ICameraController` (Hik/Dahua/Milesight) chỉ đọc `Channel` MỘT LẦN trong constructor, không nơi nào refresh lại; fix: `SettingsWindow.axaml.cs` EditClicked — Stop()+Start() lại camera nếu đang có `cameraController` active sau khi lưu | `camera-integration/ipgs-camera-controller-stale-channel-after-edit.md` |
| 48 | vietanh / parking-v8-app-avalonia (BUG-004 vòng 3 — camera thật) | 2026-07-20 | Lỗi | ⚠️ CRITICAL: User báo RAM tăng nhanh không giảm CHỈ khi camera thật kết nối (không xảy ra khi test không camera) — root cause: `AnvPlayerService.ConsumerMotionDetectionAsync/ConsumerAiDetectionAsync` cấp phát `bmp.Copy()` cho event `MotionAnalyzing`/`AIDetectAnalyzing` KHÔNG CÓ subscriber nào trong toàn app (grep xác nhận 0 kết quả) → mỗi lần LPR phát hiện biển số mới/AI box đổi trạng thái là leak 1 SKBitmap native; fix: dispose bitmap trong try/finally ngay sau khi Invoke event | `camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md` |
| 49 | kienpt / iPGSv4 (migrate-ipgs-kiosk-avalonia, wave 2) | 2026-07-20 | Lỗi | VS báo lỗi build "exceeds the OS max path limit" khi mở `IPGS.Kiosk.Avalonia` — root cause: lệnh `cp` chạy sai quoting ở task 2.A3 biến cả dòng lệnh thành 1 tên file/thư mục siêu dài (NTFS map ký tự đặc biệt sang Private Use Area) nằm trong `Assets/Fonts` bị `<AvaloniaResource Include="Assets\**">` glob, MSBuild crash evaluate `%(FullPath)`; fix: xóa bằng Python `os.remove` với prefix `\\?\` (path thường >260 ký tự không xóa được) | `dotnet-general/garbled-filename-msbuild-maxpath.md` |
| 50 | vietanh / parking-v8-app-avalonia (BUG-004 vòng 4-5 — camera thật) | 2026-07-20 | Lỗi | ⚠️ CRITICAL: RAM vẫn tăng ~35-40MB/s sau fix vòng 3 kể cả không bật motion — instrument `GC.GetTotalMemory()` vs `Process.PrivateMemorySize64` xác nhận leak là NATIVE (managed heap phẳng ~25-32MB), không phải SKBitmap/pool. Instrument tiếp theo per-instance phát hiện 2/3 camera VĨNH VIỄN `Bounds=(0,0)` (tile nằm trong tab/lane không hiển thị, Avalonia TabControl lazy layout không bao giờ Measure) → decode ở native resolution (1280×720) liên tục vô thời hạn; fix cap fallback về 640×360 giảm tốc độ tăng ~50% (còn ~18-19MB/s, phần leak "cố định/frame" còn lại cần profiler native VMMap/WinDbg) | `camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md` |
| 51 | vietanh / parking-v8-app-avalonia (BUG-004 vòng 6 — test harness cô lập) | 2026-07-20 | Pattern | Tạo test harness `CameraDemoWindow` (ParkingV8.DemoHost) fix cứng 1 camera test (video file cục bộ) + auto-connect để tái dùng test RAM nhanh không cần login/multi-lane. Test cô lập 1 camera vẫn leak ~7.4MB/s → loại trừ multi-camera/tab-ẩn/motion. Instrument log địa chỉ `bmp.GetPixels()` mỗi lần SKBitmapPool dequeue lại object CHỨNG MINH (99/100 lần địa chỉ ổn định 100%) SKBitmap/pool KHÔNG phải nguồn leak — leak nằm hoàn toàn trong native FFmpeg (`av_frame_unref`/AVFrame buffer), vượt quá khả năng debug qua code C#, cần VMMap/WinDbg |
| 52 | vietanh / parking-v8-app-avalonia (BUG-004 vòng 11-12 — thay SKBitmap bằng GDI+ để so sánh) | 2026-07-20 | Pattern | ⚠️ XÁC NHẬN DỨT KHOÁT: thêm nhánh convert tạm bằng System.Drawing.Bitmap+LockBits (y hệt WinForms) NGAY trong process Avalonia, cùng sws_scale/file test — RAM ổn định TUYỆT ĐỐI 164MB/20s (không tăng), loại trừ hẳn FFmpeg/sws_scale/FFmpegLoader/render. Leak nằm 100% trong SKBitmap (SkiaSharp 4.150.1, bị pin cứng bởi ReadPL+Kztek.Object.MultyPlatform không hạ version được để test thêm). Bài học phương pháp: khi nghi leak 1 module, thay THẲNG bằng implementation khác đã biết KHÔNG leak, chạy cùng pipeline/dữ liệu — cách nhanh nhất cô lập mà không cần profiler | `camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md` |
| 53 | vietanh / parking-v8-app-avalonia (BUG-004 FIX C2 — bypass SKBitmap toàn pipeline) | 2026-07-20 | Tính năng | ✅ FIX ÁP DỤNG: đổi toàn bộ decode pipeline (display+motion+AI channel) từ SKBitmap/SKBitmapPool sang Avalonia WriteableBitmap/AvaloniaFramePool (file mới) — SKBitmap chỉ còn ở 2 điểm bắt buộc (ReadPL.Recognize/MolexDetection.Predict, convert on-demand tần suất thấp). Verify: RAM ổn định 185-195MB/40s (trước ~6-8MB/s). Đổi hợp đồng GetCurrentVideoFrame() SKBitmap trả về: từ "tham chiếu sống cấm dispose" → "bản sao phải dispose" — đã sửa 3 call site (CameraView, CameraFocusWindow). QUYẾT ĐỊNH KHÔNG mở rộng sang ParkingV8.Lpr (dùng SKBitmap kiểu cấp phát-dùng 1 lần-giải phóng, không phải mẫu hình leak) và code chết (frmRegions/frmViewCamera/HwVideoStreamDecoder — không bao giờ chạy) | `camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md` |
| 54 | vietanh / Kztek.Camera BaseLIB (BUG-004 — commit lib, phát hiện call site sót) | 2026-07-20 | Lỗi | Trước khi commit code camera lib, đọc lại diff phát hiện `frmViewCamera.axaml.cs` (`OnTimerTick`, timer 300ms) vẫn gọi `GetCurrentVideoFrame()` theo hợp đồng CŨ ("không dispose") — do file này bị sửa song song bởi nhánh Milesight camera + watchdog, không được cập nhật theo hợp đồng mới C2 → leak 1 SKBitmap copy/300ms. Fix: thêm `using`. Bài học: đổi ownership contract của API dùng chung PHẢI grep lại TOÀN BỘ call site trong solution ngay trước khi commit, không chỉ dựa vào danh sách đã biết từ đầu task | `camera-integration/anvplayerservice-motionanalyzing-unsubscribed-bitmap-leak.md` |
| 61 | dungnn / iPGSv4 (migrate-ipgs-kiosk-avalonia, audit ucPaymentInfo) | 2026-07-21 | Lỗi | remain=0 → ContentControl blank + GoHome không điều hướng về SelectLanguage | `avalonia/avalonia-paymentinfo-remain-zero-gohome-pattern.md` |
| 62 | dungnn / iPGSv4 (migrate-ipgs-kiosk-avalonia, audit ucPaymentInfo) | 2026-07-21 | Lỗi | Missing DataTemplate → ContentControl blank; z-order sai trong Grid → BtnBack che sub-UC | `avalonia/avalonia-contentcontrol-missing-datatemplate-zorder.md` |
| 63 | dungnn / iPGSv4 (LocateVehicleView) | 2026-07-21 | Lỗi | Nút "Quay lại" bấm vào chữ không có tác dụng — gốc WinForms wire Click cho cả Label lẫn PictureBox icon riêng, port chỉ wire icon nhỏ | `avalonia/avalonia-back-button-only-icon-wired-not-label.md` |
| 55 | kienpt / iPGSv4 (migrate-ipgs-kiosk-avalonia, audit parity sau wave 1/2) | 2026-07-20 | Lỗi | ⚠️ CRITICAL: User tự kiểm tra UI đã migrate (build PASS, đã commit qua 9+ task) phát hiện SAI KHÁC hàng loạt so với bản gốc — root cause hệ thống: Senior/Junior Developer khi migrate tự ý đổi nền trắng gốc sang theme navy/brand KZTEK, mất màu đỏ cảnh báo `#DA291C`, `ArcButton` không có hover/pressed state, nút icon PictureBox-hover-swap-image bị thay bằng nút text đơn giản, vài label ASCII thay tiếng Việt có dấu — "build PASS 0 error" hoàn toàn không phát hiện được vì đây là lỗi trực quan không phải lỗi biên dịch; fix: audit riêng đọc song song `.Designer.cs` gốc (không chỉ `.cs`) đối chiếu màu/ảnh/vị trí, tạo control chung `HoverImageButton` port lại PictureBox-hover-swap-image | `avalonia/avalonia-migration-theme-drift-not-caught-by-build-pass.md` |
| 56 | trongtv / iPGSv4 (migrate-ipgs-kiosk-avalonia, audit parity nhóm E + wave 2 dở) | 2026-07-20 | Lỗi | Bảng màu gốc cụ thể để tái dùng (lblTime #9EA1A2, value #2A2F30/#251C53, nút phụ ControlLight #E3E3E3+Black, Invoice #251C53, CTA #F05922, còn-lại đỏ #DA291C) + gotcha: KzKeyboard (bàn phím ký tự) KHÔNG khớp ucSearchNumber (dialpad SỐ tròn) → build control riêng, bắt click bằng AddHandler(Button.ClickEvent, Bubble) ở root (không GetVisualDescendants trong ctor); port dở hay lỗi AVLN2000 (thiếu .axaml.cs) và {x:Static BoolConverters.False} không tồn tại | `avalonia/avalonia-port-must-read-designer-backcolor-not-assume-theme.md` |
| 57 | kienpt / iPGSv4 (migrate-ipgs-kiosk-avalonia, user báo chọn ngôn ngữ không chuyển trang) | 2026-07-20 | Lỗi | ⚠️ CRITICAL: User test chọn ngôn ngữ xong bấm không chuyển màn — root cause: `WindowNavigationService.NavigateTo()` (MainWindow.axaml.cs) thiếu hẳn case `"SelectVehicleType"`, `AppRoutes.CarBranch`, `AppRoutes.MotorBranch` vì mỗi task migrate song song (2.B3/B4/C-nhóm/D-nhóm) chỉ tự thêm case cho màn hình ĐÍCH của mình, không ai phụ trách case cho route DẪN VÀO (entry point); `default:` chỉ log "Unknown route" rồi im lặng giữ nguyên màn hình — build PASS, không crash, chỉ phát hiện được khi test tay đúng luồng; fix: đối chiếu toàn bộ AppRoutes constants + NavigateTo("...") string literal với case đã implement | `avalonia/avalonia-navigation-switch-missing-entry-routes.md` |
| 58 | ANHNV_2025 / parking-v8-app-avalonia (double-click focus → frmViewCamera, full-res snapshot, RAM investigation) | 2026-07-20 | Lỗi | ⚠️ CRITICAL: Sau khi đổi `Camera.GetCurrentVideoFrame()` sang đọc `_lastFrame` (native res, WriteableBitmap, bỏ SKBitmap) để phục vụ double-click focus window mới (`frmViewCamera`, thay `CameraFocusWindow` tự viết polling+JPEG) — cửa sổ focus đứng hình sau ~8s. Root cause: `_lastFrame` (`VideoStreamDecoderIntptr`) là field VESTIGIAL — `CloneFrame()` định nghĩa sẵn nhưng 0 call site populate nó trong decode loop mới, luôn `IntPtr.Zero` → watchdog `frmViewCamera` (8s không có frame → Stop player) false-positive dừng hẳn player. Fix: populate `_lastFrame` bằng `CloneFrame(_pFrame)` (av_frame_ref, rẻ, không copy pixel) ngay trong decode loop. User xác nhận double-click hoạt động đúng + RAM ổn định sau fix | `camera-integration/anvplayer-lastframe-dead-field-watchdog-false-stop.md` |
| 59 | ANHNV_2025 / parking-v8-app-avalonia (ReadPL 1.1.2 tensor API + loại SkiaSharp toàn thư viện camera + ParkingV8.Lpr) | 2026-07-20 | Tính năng | ✅ FIX LỚN: User cung cấp `WriteableBitmapConverter.Extract()` (WriteableBitmap→ReadPL.ImageTensorResult) sau khi tự nâng ReadPL 1.0.8→1.1.2 (API đổi hẳn sang tensor-based: `Recognize(float[], w, h)`, không còn nhận SKBitmap). Wire vào `AnvPlayerService.plateReader.Recognize`, chuyển `MolexDetection` (AI open/close) sang WriteableBitmap (letterbox bilinear tay, không dùng SKCanvas), xóa `SKBitmapPool`/`ConvertFrameToBitmap`/`GetFullCurrentFrame` (dead code sau BUG-004), xóa 5 file UI chết (frmRegions/RegionsEditorControl/SnapshotSource/HwVideoStreamDecoder — 0 caller; GIỮ LẠI `frmViewImage` vì user xác nhận vẫn dùng, đã convert riêng), gỡ hẳn `<PackageReference SkiaSharp>` khỏi `Kztek.Cameras.Avalonia.csproj`. Cascade sang `ParkingV8.Lpr` (KztekLprStandardlone/AmericalLpr/KztekLPRAIServer, ILpr, LicensePlate, KioskDialogRequest) vì NuGet version-unify ReadPL 1.1.2 lên toàn solution làm vỡ build ở các engine chưa cập nhật — dùng `WriteableBitmap.Decode(stream)` (Avalonia API thuần, không cần SkiaSharp) để bridge Bitmap→pixel. Build 0 lỗi toàn bộ (lib + App/UI/Configuration/DemoHost/Lpr) | `camera-integration/anvplayer-lastframe-dead-field-watchdog-false-stop.md` |
| 60 | ANHNV_2025 / parking-v8-app-avalonia (loading screen InitLprEngine chỉ là UI placeholder + AI_ACTIVE_CODE chưa từng gán) | 2026-07-20 | Lỗi | ⚠️ CRITICAL (2 lỗi liên quan): (1) Bước "InitLprEngine" trong `AppStartupLoader.cs` chỉ đếm endpoint hiển thị + `Task.Delay(150)` giả — KHÔNG thực sự gọi `LprFactory.CreateLprDetecter().CreateLprAsync()`; fix: gọi thật, cập nhật state Warning nếu lỗi. (2) `Kztek.Cameras.Camera.AI_ACTIVE_CODE` (static field) chưa từng được gán ở app thật (chỉ gán tay trong tool test nội bộ MotionGateTest) — `AnvPlayerService.plateReader` là `static readonly`, chỉ đọc field này 1 LẦN lúc class chạm đầu tiên → activation code luôn rỗng, ReadPL chạy degraded mode im lặng (không exception, không log lỗi) từ trước tới nay. Fix: gán `Camera.AI_ACTIVE_CODE = configuration.Lpr.AIActiveCode` NGAY sau khi load config, trước mọi code chạm namespace Kztek.Cameras | `camera-integration/camera-ai-active-code-static-readonly-never-set.md` |
| 61 | trongtv / iPGSv4 (migrate-ipgs-kiosk-avalonia, user báo lỗi SQL rỗng + dropdown Controller Type rỗng + yêu cầu border none) | 2026-07-21 | Lỗi | ⚠️ CRITICAL: Copy `config.txt` thật từ production WinForms sang test Avalonia → toàn bộ ô SQL Server/Database rỗng dù file JSON có dữ liệu. Root cause: `KioskAppConfig.cs` (Avalonia) đặt tên property "đẹp" (`DbServer`, `RabbitMqHost`...) thay vì đúng tên field JSON gốc dùng chung với WinForms (`PGSDbServer`, `RabbitMQUrl`, `PasskingPassword`...) — Newtonsoft không tự suy luận ánh xạ tên khác hẳn nhau. Đồng thời `ControllerType`/`CardFormat`/`QRMethod` là enum serialize dạng int ordinal trong JSON gốc nhưng model Avalonia định nghĩa là string → dropdown "Loại bộ điều khiển" rỗng. Fix: thêm `[JsonProperty("TenGoc")]` giữ đúng tên JSON gốc (kể cả lỗi chính tả cố ý), đổi field enum về `int`, quy đổi ordinal↔label ở ViewModel bằng bảng tra cứu đúng thứ tự enum gốc. Kèm fix phụ: thêm `SystemDecorations="None"` cho MainWindow/ConnectionConfigWindow/VerifyPasswordWindow (bản gốc WinForms `FormBorderStyle.None` toàn bộ, port Avalonia thiếu sót để lộ title bar OS) | `avalonia/avalonia-config-json-property-name-mismatch-winforms-shared-file.md` |
| 62 | trongtv / iPGSv4 (migrate-ipgs-kiosk-avalonia, user báo countdown không reset khi bấm item/màn hình) | 2026-07-21 | Lỗi | ⚠️ CRITICAL, cross-cutting 9 màn hình (CarInforDetail, MotorInforDetail, LocateVehicle, MotoPaymentComplete, BikePaymentResult, CarVehicleResult, MotorVehicleResult, Invoice, MotoVoucher): countdown auto-return-về-Main không reset khi user click/thao tác, tự động "đá" người dùng ra giữa lúc đang thao tác. Root cause: bản gốc dùng `KztekComponent.Controls.KzRoundCountdown.TargetControl` — setter hook `MouseDown` ĐỆ QUY lên Form + TOÀN BỘ control con (kể cả control thêm động qua `ControlAdded`) để bất kỳ click nào cũng Reset()+Start() lại; khi port sang Avalonia, không có sẵn control này trong `ParkingV8.UI` nên mỗi ViewModel tự viết `DispatcherTimer` đếm lùi thủ công — countdown vẫn chạy đúng nhưng KHÔNG AI port lại cơ chế "hook mọi click để reset" → hoàn toàn điếc với tương tác người dùng, không lỗi build/exception nên chỉ lộ ra khi thao tác tay đủ lâu. Fix: tạo interface `IResettableCountdown`, mọi ViewModel/View có countdown implement + expose `public ResetCountdown()`; bắt 1 handler `PointerPressed` duy nhất ở tầng Tunnel tại `MainWindow` (route qua trước khi control con xử lý, không bị chặn bởi `e.Handled`), check `MainContent.Content` và `.DataContext` có implement interface không — chỉ cần wire 1 chỗ, áp dụng cho mọi màn hình swap qua ContentControl | `avalonia/avalonia-countdown-reset-on-click-missing-targetcontrol-hook.md` |
| 63 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user báo VS debugger break "Exception User-Unhandled" tại `SocketServerImpl.AcceptLoop`, dòng `_listener.Accept()`) | 2026-07-21 | Lỗi | `SocketServerImpl.AcceptLoop()` chạy `Socket.Accept()` (đồng bộ, blocking) trong `Task.Run(..., ct)`; `StopAsync()` hủy `ct` rồi `Close()` listener để đánh thức thread đang block — trên Windows việc này ném `SocketException` ("A blocking operation was interrupted by a call to WSACancelBlockingCall"), KHÔNG PHẢI `OperationCanceledException`, nên rơi vào `catch (Exception)` chung khiến VS coi là lỗi thật và break debugger giữa luồng dừng server hoàn toàn bình thường. Fix: bắt riêng `catch (SocketException) when (ct.IsCancellationRequested)` + `catch (ObjectDisposedException)` để `break` êm; bọc `Task.Delay(500, ct)` trong catch chung bằng try/catch `OperationCanceledException` tránh exception thoát ra ngoài `AcceptLoop` khi cancel xảy ra giữa lúc delay | `networking-protocol/socket-accept-close-wsacancelblockingcall.md` |
| 64 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user gửi lại screenshot xác nhận VS VẪN break sau bản fix #63) | 2026-07-21 | Gotcha | ⚠️ CẬP NHẬT lesson #63: đặt `catch (SocketException) when (ct.IsCancellationRequested)` ở tầng NGOÀI bao quanh `await Task.Run(() => _listener.Accept(), ct)` KHÔNG đủ — VS debugger's "break when user-unhandled" không phân tích tin cậy exception filter (`when`) khi exception phải propagate qua ranh giới `Task.Run`→`await`/thread pool, nên vẫn break dù runtime sẽ catch đúng. Fix ĐÚNG: chuyển catch vào NGAY TRONG lambda đồng bộ của `Task.Run` (cùng stack frame với nơi `Accept()` ném ra), trả về `null` khi bị cancel/dispose, tầng `AcceptLoop` kiểm tra `client is null` để `break`/`continue`. Nguyên tắc rút ra: catch càng gần nơi throw (cùng stack frame đồng bộ) càng đáng tin cậy với debugger/công cụ phân tích tĩnh khi exception phải băng qua async/thread-pool boundary | `networking-protocol/socket-accept-close-wsacancelblockingcall.md` |
| 65 | ANHNV_2025 / parking-v8-app-avalonia (BUG-005, user báo camera phát mờ, sai độ phân giải, giật) | 2026-07-21 | Lỗi | ⚠️ CRITICAL: `AnvPlayerService.PollingDecodeFrameAsync()` decode camera theo Uniform-fit (giữ tỷ lệ khung hình gốc, `Math.Min(ctrlW/srcW, ctrlH/srcH)`) nhưng `AnvPlayer.Render()` vẽ Stretch=Fill CÓ CHỦ Ý (lấp đầy control, không letterbox — khớp WinForms `e.Graphics.DrawImage(bmp, ClientRectangle)`) → khi tỷ lệ khung hình camera khác tỷ lệ control, `decodeW/decodeH` nhỏ hơn `ctrlW/ctrlH` thực tế → Render kéo giãn thêm lần 2 (GPU-side, ngoài `sws_scale`) → 2 lớp scale không khớp (downscale sws rồi upscale Avalonia) → ảnh mờ + độ phân giải hiển thị không đúng dữ liệu decode. Fix: đổi decode sang Fill khớp chính xác `ctrlW×ctrlH` (non-uniform) — `sws_scale` (FFmpeg) là bước resize DUY NHẤT, giữ nguyên yêu cầu user "phải dùng resize bằng sws của ffmpeg" | `camera-integration/camera-decode-uniform-fit-vs-render-stretch-fill-blur.md` |
| 66 | dungnn / Kztek.Camera BaseLIB WinForms (BUG-WF-001, user báo camera thỉnh thoảng đứng hình) | 2026-07-22 | Lỗi | ⚠️ HIGH: `VideoStreamDecoderIntptr.Dispose()` không gọi `TryComplete()` trên FrameChannel/MdChannel/AiChannel writer → khi RTSP drop + reconnect, consumer `ConsumerDisplayFrameAsync` bị stuck mãi trong `ReadAllAsync` của channel cũ (writer chưa bao giờ completed, token chưa cancelled) → UI đứng hình vô thời hạn dù luồng mới đã kết nối lại thành công. Fix: thêm 3 dòng `TryComplete()` vào đầu Dispose() | `camera-integration/channel-reader-stuck-missing-trycomplete-on-dispose.md` |
| 66 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user gửi screenshot VS debugger break `NullReferenceException` tại `SystemUtils.logger.SaveAPILogDetail`) | 2026-07-21 | Lỗi | `IPGS.Kiosk.Avalonia/App.axaml.cs` (entry point mới sau migrate) quên copy dòng khởi tạo `SystemUtils.logger = LoggerFactory.CreateLoggerService(...)` mà bản WinForms gốc `IPGS.Kiosk/Program.cs:26` có làm trước khi tạo bất kỳ API service — lần gọi API đầu tiên (`SaveAPILogDetail` trong `Kztek.Api.MultyPlatform.dll`, gọi thẳng không `?.`) crash NullReferenceException. Khác biệt so với lesson gốc `iparkingv8-api-systemutils-logger-null.md`: project Avalonia reference `Kztek.Tool.MultyPlatform` (không phải `Kztek.Tool` gốc) — `LoggerFactory` nằm namespace RIÊNG `Kztek.Tool.LogHelpers`, cần thêm `using` riêng. Fix: thêm `SystemUtils.logger = LoggerFactory.CreateLoggerService(EmLogServiceType.OFFLINE_DB, AppContext.BaseDirectory);` làm dòng đầu `OnFrameworkInitializationCompleted()` + `using Kztek.Object;`/`using Kztek.Tool.LogHelpers;`. Build xác nhận 0 Error | `avalonia/avalonia-systemutils-logger-null-multyplatform.md` |
| 67 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user yêu cầu "bật dev tool Avalonia, bấm F12 để mở") | 2026-07-21 | Lỗi | Core đã lên Avalonia 12.1.0 nhưng package `Avalonia.Diagnostics` KHÔNG có bản 12.x nào trên NuGet (dừng ở 11.3.18, khác mọi sub-package Avalonia khác đều đã 12.1.0) → NU1102 khi ghim version khớp core. Đối chiếu `parking-v8-app-avalonia/src/ParkingV8.App.csproj` (cùng tổ chức, cũng đã lên core 12.1.0) phát hiện họ đã đổi sang package `AvaloniaUI.DiagnosticsSupport` (versioning độc lập, hiện 2.2.3, kiến trúc DevTools out-of-process) — nhưng bên đó MỚI CHỈ khai báo package, CHƯA gọi wiring nào (grep xác nhận 0 call site). API cũng đổi tên: `this.AttachDevTools()` (cũ, gọi trên `Application`) → `.WithDeveloperTools()` (mới, gọi trên `AppBuilder` trong `Program.cs.BuildAvaloniaApp()`, namespace `Avalonia.DeveloperToolsExtensions`). Fix: thêm `PackageReference AvaloniaUI.DiagnosticsSupport 2.2.3` (Condition Debug) + `builder.WithDeveloperTools()` trong `BuildAvaloniaApp()` bọc `#if DEBUG`. Build xác nhận 0 Error. Tra API thật của package chưa quen bằng cách đọc chuỗi ASCII trong DLL tải về `~/.nuget/packages/` qua PowerShell khi không có doc rõ ràng | `avalonia/avalonia-devtools-package-migration-v12.md` |
| 68 | dungnn / iPGSv4 (migrate-ipgs-kiosk-avalonia, click item MotorVehicleResult không điều hướng sang MotorInforDetail) | 2026-07-21 | Lỗi | `MotorVehicleResultViewModel.ItemClicked` tính `remain = TotalAmount - PaidAmount` từ `PaymentVehicleDto` đã null-coalesce về 0 khi API trả `null` (stub/lỗi) → `remain=0` bị hiểu nhầm là "xe đã thanh toán đủ" → navigate sai sang `BikePaymentResult`. Nguyên tắc: `null` = "chưa biết", KHÔNG phải "=0". Fix: dùng `detail?.IsPaid` làm điều kiện điều hướng; `null` → mặc định `MotorInforDetail`. Áp dụng tương tự cho `LocateVehicleViewModel` (skip result list khi count==1) và `SearchMotoEntriesAsync` (card swipe, cần `Dispatcher.UIThread.InvokeAsync` vì chạy trên non-UI thread) | `avalonia/avalonia-navigation-null-api-response-wrong-route.md` |
| 69 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user báo "chuyển màn không phát âm thanh" trên Windows) | 2026-07-22 | Lỗi | `.csproj` chỉ khai báo `<AvaloniaResource Include="Assets\**">` — chỉ nhúng file vào assembly (`avares://`), KHÔNG copy file vật lý ra `bin/`. `SoundServiceImpl` (`System.Media.SoundPlayer`) và `SoundServiceLinux` (mới thêm, `paplay/aplay/ffplay`) đọc bằng physical path (`AppPathService.SoundRoot` = `AppContext.BaseDirectory` + `Assets/Sound`) → file không tồn tại → `FileNotFoundException` bị catch-log-only, im lặng không phát âm thanh, build vẫn PASS 0 Error. `PrinterServiceImpl` (đọc `Ticket.html` qua `TicketTemplatePath`) mắc lỗi tương tự. Fix: thêm `<Content Include="Assets\Sound\**;Assets\Templates\**"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></Content>` song song với `AvaloniaResource` (không áp dụng cho Fonts — chỉ dùng qua `avares://`, không cần copy vật lý). Verify bằng cách kiểm tra `bin/Debug/net8.0/Assets/Sound/*.wav` tồn tại sau build, không chỉ tin build PASS | `avalonia/avalonia-avaloniaresource-not-copied-to-output.md` |
| 70 | ANHNV_2025 / iPGSv4 (user báo màn hình CarInforDetail/MotorInforDetail — dòng cảnh báo và khối biển số/tầng/cột lệch trái, icon blink làm chữ dịch chuyển) | 2026-07-22 | Lỗi | `Canvas` bỏ qua `HorizontalAlignment`/`VerticalAlignment` của con TRỰC TIẾP (Arrange rect luôn khít `DesiredSize`, không có khoảng dư để alignment phát huy) — `StackPanel`/`Grid` là con trực tiếp Canvas dù set `Width="1080" HorizontalAlignment="Center"` vẫn dồn về mép trái. Bug lặp lại 2 lần trong cùng session vì vòng 1 chỉ sửa `StackPanel` cảnh báo, bỏ sót `Grid` biển số/tầng/cột ngay bên dưới cùng nguyên nhân. Fix: bọc thêm 1 `Grid` trung gian (Canvas.Left/Top/Width giữ nguyên ở lớp ngoài, `HorizontalAlignment="Center"` chuyển vào lớp trong); icon ẩn/hiện (blink) dùng `ColumnDefinitions` pixel cố định (không `Auto`) để cột luôn giữ chỗ, tránh nội dung khác dịch chuyển khi `IsVisible` toggle | `avalonia/avalonia-canvas-direct-child-horizontalalignment-ignored.md` |
| 71 | ANHNV_2025 / iPGSv4 (user báo "thiếu logo" trên MotorInforDetailView) | 2026-07-22 | Lỗi | `AppState.LogoPath/SubLogoPath/AdsPath` có comment "Set từ KioskAppConfig khi App.axaml.cs khởi động" nhưng grep thực tế xác nhận `App.axaml.cs` KHÔNG BAO GIỜ gán 3 dòng này (trong khi các field khác như `IsShowInvoiceForm`/`ParkingApiUrl`/`VisaPosId` đều wire đầy đủ cùng vị trí) — lỗi ảnh hưởng MỌI view toàn app dùng `AppState.SubLogoPath`, không exception (do `File.Exists("")` chỉ trả false), build PASS. Fix: thêm `AppState.LogoPath = kioskConfig.MainLogoPath; AppState.SubLogoPath = kioskConfig.SubLogoPath; AppState.AdsPath = kioskConfig.AdsPath;` vào `App.axaml.cs` cạnh nhóm gán AppState khác. Bài học: comment "set từ X" trong file model KHÔNG PHẢI bằng chứng code đã làm — luôn grep `AppState\.\w+\s*=` để verify wiring thật | `avalonia/avalonia-appstate-logopath-never-wired.md` |
| 72 | ANHNV_2025 / IPGS.Kiosk.Avalonia (user báo "app không tự mở lại sau khi sửa config") | 2026-07-22 | Lỗi | ⚠️ HIGH: Bản gốc WinForms (`frmMain.cs`) gọi `Application.Restart()` sau khi lưu cấu hình kết nối để app tự đóng/mở lại áp dụng config mới. Port Avalonia (`MainView.axaml.cs OnSettingClicked`) chỉ gọi `Environment.Exit(0)` kèm comment giả định "để OS/supervisor khởi động lại" — giả định sai vì kiosk không chạy dưới bất kỳ supervisor/service nào, nên app tắt hẳn và đứng yên, không tự mở lại. Fix: thêm `RestartApplication()` — `Process.Start(new ProcessStartInfo(Environment.ProcessPath) { UseShellExecute = true })` trước `Environment.Exit(0)`, tự spawn lại chính executable (tương đương `Application.Restart()` WinForms, pattern giống `exePath` đã dùng ở `App.axaml.cs` cho autostart) | `avalonia/avalonia-application-restart-missing-after-config-save.md` |
| 73 | ANHNV_2025 / iPGSv4 ZcuAgent (CCU không xem được màn hình sau khi ZCU xoay portrait) | 2026-07-23 | Lỗi | `X11ScreenCapturer.ScreenSize` cache một lần trong `Initialize()` — khi user dùng `xrandr` đổi resolution/orientation lúc agent đang stream, SHM buffer (kích thước cũ) và `XGetImage` (w/h cũ) request vượt drawable thực tế → X error / ảnh đen ở CCU. Fix: re-query `XDisplayWidth/Height` mỗi frame (Xlib client-side cached, không round-trip, chi phí không đáng kể), nếu khác → cập nhật `ScreenSize`, nếu đang dùng SHM → `CleanupSHM()` + `TryInitSHM(newW, newH)`, thất bại → `_useSHM = false` (fallback XGetImage). Không cần lock vì `Capture()` và `Initialize()` cùng thread (per class contract). `CapturedFrame.Width/Height` tự đúng vì đọc từ `XImageHeader` thực tế, không từ cache | `camera-integration/x11-screencapturer-stale-screensize-on-resolution-change.md` |
| 73 | ANHNV_2025 / iPGSv4 (user báo màn hình CarInforDetail/MotorInforDetail — chữ vẫn "nhảy" lên xuống theo nhịp icon blink sau fix lệch trái) | 2026-07-22 | Lỗi | Sau khi fix lệch trái (entry #70), `Grid` chứa icon+chữ vẫn KHÔNG khai báo `RowDefinitions` (single implicit row Auto) — hàng vẫn tự co giãn theo chiều cao lớn nhất giữa Image (46px, biến mất khi `IsVisible=false`) và TextBlock (~40-44px do font CJK), khiến `VerticalAlignment="Center"` lệch vài px mỗi 500ms theo nhịp `timerBlink`, mắt người thấy như "nhảy" liên tục dù biên độ nhỏ. Fix: ép `RowDefinitions="46"` cố định (bằng đúng chiều cao icon) cho Grid ngoài lẫn Grid trong, loại bỏ hoàn toàn dao động chiều cao hàng | `avalonia/avalonia-canvas-direct-child-horizontalalignment-ignored.md` |
| 74 | ANHNV_2025 / iPGSv4 (user xác nhận đã rebuild+restart app thật nhưng ảnh xe vẫn không bo góc VÀ icon cảnh báo vẫn mất hẳn) | 2026-07-22 | Lỗi | 2 fix: (1) `Border` dùng `Image` làm Child + `ClipToBounds="True"` không chắc chắn clip theo `CornerRadius` — đổi sang `ImageBrush` làm `Border.Background` (Border luôn tự clip phần vẽ Background theo đúng CornerRadius, cơ chế core rendering, không phụ thuộc hành vi clip-con). (2) Icon vẫn mất dù binding/timer/build đều đúng và đã rebuild+restart thật (user xác nhận rõ) → mở `icon_infor.png` bằng Pillow phát hiện file là placeholder **1×1 pixel alpha=0** sót lại từ migration — build không có cơ chế phát hiện vì file hợp lệ định dạng PNG. Trích icon thật từ `.resx` gốc WinForms (`frmCarInforDetail.resx`, key `picInfor.Image`, base64) — quét thêm toàn bộ `Assets/` tìm placeholder cùng loại, phát hiện thêm `icon_success.png` (trích từ `frmBikePaymentResult.resx`, key `pic.Image`) | `avalonia/avalonia-1x1-placeholder-icon-asset-never-replaced.md` |
| 77 | ANHNV_2025 / iPGSv4 (user hỏi "thanh toán xong thì gọi dừng countdown thế nào?" trên `MotoVisaTransactionIdViewModel`) | 2026-07-22 | Lỗi | Lesson mới: route dialog-overlay `AppRoutes.MotoPaymentComplete` (`ShowMotoPaymentCompleteDialogAsync`) KHÔNG swap `MainContent` → `DetachedFromVisualTree` của view thanh toán phía sau (nơi có `StopCountdown()`) KHÔNG BAO GIỜ fire khi thanh toán thành công → `KzRoundCountdown` vẫn tick ngầm sau dialog, nếu đủ lâu sẽ tự kích hoạt `GoBackCommand` dù đã thanh toán xong. Fix: thêm `vm.PropertyChanged` subscription trong `MotoVisaTransactionIdView.axaml.cs` — khi `IsPaymentSuccess` chuyển `true` thì gọi `StopCountdown()` tường minh, không dựa vào lifecycle detach. Nghi vấn cùng bug: `MotoQrImageViewModel`/`MotoQrImageView` (cấu trúc countdown giống hệt, chưa verify) | `avalonia/avalonia-dialog-overlay-route-does-not-detach-underlying-view-timer-leak.md` |
| 78 | ANHNV_2025 / IPGS.Kiosk.Avalonia (WF-REFACTOR: replace DB polling with in-process RabbitMQ notification) | 2026-07-22 | Lỗi | 2 lỗi build khi implement RabbitMQ auto-recovery trên `IConnection`: (1) `CS0246: IAutorecoveringConnection not found` — trong RabbitMQ.Client v7.2.1 recovery events đã được đưa lên thẳng `IConnection` interface, không cần cast; (2) `CS0123: handler không match AsyncEventHandler<AsyncEventArgs>` — `RecoverySucceededAsync` dùng `AsyncEventArgs` từ `RabbitMQ.Client.Events`, KHÔNG phải `System.EventArgs`. Thêm: `TopologyRecoveryEnabled=true` tự re-subscribe consumer sau recovery, không cần gọi lại `BasicConsumeAsync` thủ công. | `dotnet-general/rabbitmq-client-v7-recovery-event-api.md` |
| 76 | ANHNV_2025 / iPGSv4 (user báo `MotoVisaTransactionIdView` bấm "Quay lại" không có tác dụng) | 2026-07-22 | Lỗi | Cập nhật lesson #75 (`avalonia-goback-history-reenters-autonaviate-waiting-screen.md`) — xác nhận đúng nghi vấn đã ghi sẵn trong mục "Áp dụng lại": `MotoVisaTransactionIdViewModel.GoBack()` gọi thẳng `_nav.GoBack()`, pop lại route `"MotoVisaTransactionId"` (=`MotoWaitingVisaView`), attach lại tự tạo giao dịch Visa MỚI rồi tự `NavigateTo(MotoVisaTransactionIdDisplay,...)` — bấm back trông như vô tác dụng, y hệt case QR (entry #75). Fix: đổi `_nav.GoBack()` → `_nav.NavigateTo(AppRoutes.MotorBranch, _param.Vehicle)` (navigate thẳng, bỏ qua history) thay vì pattern event `BackRequested` — ViewModel này đã tự giữ `INavigationService _nav` sẵn nên không cần MainWindow wiring hộ | `avalonia/avalonia-goback-history-reenters-autonaviate-waiting-screen.md` |
| 75 | ANHNV_2025 / iPGSv4 (user báo màn `MotoQrImageView` — Sai UI, bấm back không tác dụng, không hiển thị QR) | 2026-07-22 | Lỗi | 3 fix cùng lúc: (1) Sai UI — 6 TextBlock giá trị (PlateNumber/DateIn/TimeIn/ParkingFee/Paid/Remain) thiếu `FontWeight="Bold"` so với gốc WinForms (`NOTOSANSCJKKR_BOLD`), cùng loại lỗi đã gặp ở `MotorPaymentView` trước đó. (2) Back "vô tác dụng" — `_nav.GoBack()` pop history chung quay lại route `"MotoQrImage"` (=`MotoWaitingQrView`), nhưng view này tự `NavigateTo(MotoQrImageDisplay)` lại ngay khi attach (tạo giao dịch QR mới) → màn hình chớp rồi quay lại y hệt QR cũ, trông như back không làm gì; fix bằng event `BackRequested` (pattern có sẵn ở `MotoCashViewModel`/`MotoVoucherViewModel`) navigate thẳng về `MotorBranch`, bỏ qua history stack. (3) QR trắng — `QrCodeService.RenderToBitmap(qrInfor.QrCode)` không có fallback khi `QrCode` null (API trả rỗng), trong khi `CarQrViewModel` (nhánh ô tô, viết sau) đã có `qrInfor.QrCode ?? qrInfor.Id` — nhánh xe máy (viết trước) bị thiếu fallback này; thêm fallback tương tự | `avalonia/avalonia-goback-history-reenters-autonaviate-waiting-screen.md` |
| 78 | ANHNV_2025 / iPGSv4 (user báo "mỗi lần quay lại main view lại phải chạy lại kết nối tới parking") | 2026-07-22 | Lỗi | ⚠️ HIGH: `WindowNavigationService.NavigateToCore` (`MainWindow.axaml.cs`) tạo `new MainViewModel(this)` mỗi lần `NavigateTo("Main")`; `MainView.OnDetachedFromVisualTree` gọi `_vm.OnUnloadedAsync()` teardown Socket Server (port 100)/RabbitMQ/Controller thiết bị mỗi khi rời màn Main — mỗi lần quay lại Main (sau MỌI giao dịch) đều reconnect lại toàn bộ hạ tầng từ đầu, hiện lại overlay "Connecting to parking server...". Root cause: hạ tầng là tài nguyên cấp APP (như WinForms `frmMain` gốc — 1 instance sống suốt vòng đời), nhưng navigation kiểu swap `ContentControl.Content` lại coi ViewModel là cấp MÀN HÌNH (tạo mới mỗi lần route tới). Fix: cache `MainViewModel` làm singleton ở `WindowNavigationService` (`_mainViewModel ??= new MainViewModel(this)`), thêm guard `IsLoaded` trong `OnLoadedAsync()` (bỏ qua nếu đã load, retry nếu lỗi), bỏ hẳn lời gọi teardown khỏi `OnDetachedFromVisualTree` (chỉ giữ teardown thật trước khi restart process ở `OnSettingClicked`). Đã đánh giá riêng `CarInforDetailViewModel`/`CarLocationViewModel` (cũng recreate mỗi lần vào) — KHÔNG áp dụng cache tương tự vì API trả về phí gửi xe (`Charge`) phụ thuộc thời gian, cache sai sẽ hiển thị nhầm số tiền thanh toán; user xác nhận hành vi gọi lại API ở 2 màn này là ĐÚNG (cố ý), chỉ Main mới là bug | `avalonia/avalonia-navigation-recreated-vm-reconnects-infra-every-visit.md` |
| 80 | ANHNV_2025 / iPGSv4 ZcuAgent (user test Ubuntu thật: agent không tự phục hồi sau khi xoay màn hình, BadMatch loop vô hạn) | 2026-07-23 | Lỗi | ⚠️ GOTCHA: `XDisplayWidth`/`XDisplayHeight` là Xlib client-side MACRO đọc cache populate tại `XOpenDisplay()` — KHÔNG gửi request lên server, KHÔNG BAO GIỜ tự cập nhật sau RandR rotate/resize. Fix trước dùng macro này để detect live change là SAI HOÀN TOÀN: agent restart sau khi màn đã ở portrait → detect đúng; nhưng xoay từ portrait→landscape TRONG KHI agent chạy → BadMatch (X_GetImage error_code=8) vô hạn không tự phục hồi. Fix đúng: `XGetGeometry` (real round-trip, luôn trả kích thước CURRENT của root window), throttle mỗi 15 frame (~1 lần/giây ở 15fps) tránh overhead round-trip. | `camera-integration/x11-screencapturer-stale-screensize-on-resolution-change.md` |
| 79 | ANHNV_2025 / iPGSv4 (user test máy nhận tiền thật: "chỉ nhận sự kiện của 1 tờ tiền đầu tiên, sau đó không nhận nữa") | 2026-07-22 | Lỗi | ⚠️ CRITICAL: So sánh `CashDeviceServiceImpl.cs` (migrate, dùng `CBA9ControllersV3`) với bản gốc WinForms (`ucCarCash.cs`/`frmMotoCash.cs`, dùng `CBA9Controllers`) — cả 2 driver (cũ lẫn mới) đều tạo CHỈ 1 object `CashResult cash` khi `PollingStart()` rồi reuse xuyên suốt phiên poll (100ms/lần); cờ `IsValidMoney`/`IsRejected` trong switch-case chỉ set `= true`, KHÔNG case nào set lại `false`. Bản gốc consumer tự gọi `ClearCash(cash)` (reset `MoneyValue=0;IsValidMoney=false;IsRejected=false;...`) ngay sau khi xử lý xong 1 tờ tiền; bản migrate chỉ thêm 2 biến local `_creditFired`/`_lastEscrowDenomination` để dedup nhưng QUÊN port bước reset object dùng chung — comment code tự nhận "giữ nguyên logic cũ / parity" nhưng thực tế thiếu bước cốt lõi. Hệ quả: sau `AcceptBill()` reset `_creditFired=false`, `cash.IsValidMoney` vẫn kẹt `true` từ tờ đầu → tờ thứ 2 không bao giờ vào lại được nhánh Escrow (`!cash.IsValidMoney`). Fix: trong `OnCba9PollAsync`, ngay sau khi raise `BillReceived(IsEscrowed=false)` thêm `cash.IsValidMoney=false; cash.MoneyValue=0;`; ngay sau khi raise `DeviceError("Bill rejected")` thêm `cash.IsRejected=false; cash.MoneyValue=0; _lastEscrowDenomination=0;`. Build PASS 0 Error sau fix | `avalonia/avalonia-cash-acceptor-reused-cashresult-stale-flag.md` |
| 80 | ANHNV_2025 / iPGSv4 (user gửi screenshot màn chọn phương thức thanh toán ô tô: "sửa đang thanh toán mà back lại, ko cập nhật lại tiền đã thanh toán") | 2026-07-22 | Lỗi | ⚠️ CRITICAL: `PaymentInfoViewModel.GoBack()` (nút Back DÙNG CHUNG ở header, hiển thị cho mọi sub-flow Cash/QR/Visa/Voucher) viết `if (ActivePaymentContent is IDisposable disposable) disposable.Dispose();` nhưng grep xác nhận KHÔNG sub-ViewModel nào (`CarCashViewModel`/`CarQrViewModel`/`CarVisaViewModel`/`CarVoucherViewModel`) implement `IDisposable` — dead code từ khi viết, không lộ qua build (compile OK, `is` chỉ false) hay qua test happy-path (không chạm nhánh back giữa chừng). Hệ quả theo từng flow: Cash — `CleanUp()` (unsubscribe `CashDevice.BillReceived/DeviceError`, `Disconnect()`) không chạy → chọn lại Cash sau đó, `Connect()+PollingStart()` reset `CashResult` dùng chung trong driver (xem lesson #79) → tờ tiền đang escrow của phiên cũ mất trạng thái, không cộng vào Paid dù máy đã nuốt tiền. QR/Visa — mỗi VM có `CancellationTokenSource _cts` cho vòng poll kết quả giao dịch chạy nền, có RIÊNG 1 lệnh `GoBack()` nội bộ (link "Đổi ngân hàng") tự cancel đúng, nhưng đó là con đường back KHÁC nút header — bấm nút header thay vì link riêng thì `_cts` không bị hủy, vòng poll vẫn chạy ngầm sau khi rời màn. Fix: thêm `IDisposable` cho `CarCashViewModel` (`Dispose() => CleanUp()`, guard `_cleanedUp`), `CarQrViewModel`/`CarVisaViewModel` (`Dispose()` chỉ cancel `_cts` + dừng dot-timer, guard `_goBackCalled` sẵn có, KHÔNG gọi lại `OnPaymentCancelled` vì parent đã tự set `ActivePaymentContent=null`). `CarVoucherViewModel` không cần fix (không giữ tài nguyên). Build PASS 0 Error | `avalonia/avalonia-shared-back-button-idisposable-cast-dead-code.md` |
| 81 | ANHNV_2025 / iPGSv4 (user báo "sửa logic khi đang thanh toán tiền mặt/voucher, đã thanh toán thành công 1 phần, quay lại payment view tiền vừa thanh toán không được cập nhật") | 2026-07-22 | Lỗi | ⚠️ HIGH: 2 nguyên nhân độc lập, cả ô tô lẫn xe máy. (1) Ô tô: `CarInforDetailViewModel`/`CarLocationViewModel.LoadPaymentVehicleAsync()` dựng `PaymentVehicleDto(entry, detail.Charge)` mỗi lần re-fetch, KHÔNG truyền `detail.Amount` (tiền đã trả — API đã trả về đúng) → constructor cũ luôn `_totalPaid=0`, mất sạch phần đã thanh toán trước đó mỗi khi quay lại rồi bấm "Thanh toán" lại. Cùng gốc: `EntryDetailDto.GetRemain() => IsPaid ? 0 : Charge` bỏ qua `Amount` khi chưa `IsPaid`. (2) Xe máy: `MotoCashViewModel`/`MotoVoucherViewModel` tích lũy tiền vào field cục bộ (`_paidSoFar`/`_payment.TotalPaid`) nhưng event `BackRequested`/`PaymentCompleted` là `EventHandler?` KHÔNG tham số — handler ở `MainWindow.axaml.cs` dùng lại biến closure `vehicle`/`vehicleDto` GỐC (chụp trước khi có thanh toán một phần) để `NavigateTo(MotorBranch,...)`, bỏ qua state mới. Fix: (a) thêm tham số `paidAmount` vào ctor `PaymentVehicleDto` (Models.Payment), truyền `detail.Amount` ở 2 nơi dựng DTO ô tô; (b) sửa `GetRemain() => Math.Max(0, Charge - Amount)`; (c) đổi `EventHandler?` → `EventHandler<long>?` ở Moto Cash/Voucher, invoke kèm tổng đã trả, sửa lại handler MainWindow dựng DTO mới từ event args thay vì tái dùng closure; (d) `CarVoucherViewModel` thiếu `OnPropertyChanged(nameof(Remain))` sau `OnPaymentCompleted` (parity đã có sẵn ở `CarCashViewModel`) — bổ sung. Build PASS 0 Error | `avalonia/avalonia-payment-partial-amount-reset.md` |
| 82 | ANHNV_2025 / iPGSv4 (IPGS.RemoteControl.CcuUI — remote desktop CCU→ZCU) | 2026-07-23 | Lỗi | HIGH: `RemoteScreenViewModel.OnFrameReceived` tạo `WriteableBitmap` mới mỗi frame (10–15 fps × ~8 MB BGRA 1080p) và gán vào `CurrentFrame` property đang bind vào `Image.Source`, nhưng KHÔNG dispose bitmap cũ trước khi gán mới → native pixel buffer chỉ được giải phóng khi finalizer chạy, không kịp GC ở tốc độ ~120 MB/s allocation → RAM tăng đều đến khi GC gen2 forced. Fix: `Dispatcher.UIThread.Post(() => { var old = CurrentFrame; CurrentFrame = wb; old?.Dispose(); })` — gán mới TRƯỚC rồi dispose bản cũ trên UI thread để không race với render layer; bổ sung `CurrentFrame?.Dispose()` trong `Dispose()` của ViewModel. Áp dụng chung cho MỌI binding property kiểu Bitmap/WriteableBitmap được gán lại nhiều lần. Build PASS 0 Error | `avalonia/avalonia-writeablebitmap-binding-not-disposed-leak.md` |
| 84 | ANHNV_2025 / iPGSv4 (IPGS.RemoteControl.ZcuAgent — test thật Ubuntu 22.04 X11) | 2026-07-23 | Lỗi | CRITICAL: `XShmAttach` bất đồng bộ — trả `true` ngay, lỗi `BadAccess` đến SAU qua X error event; Xlib default handler gọi `exit()` trước khi fallback C# kịp chạy; fix: `XSetErrorHandler` tùy chỉnh (delegate `static readonly` tránh GC) + reset flag + `XSync()` sau `XShmAttach` để buộc lỗi flush ngay | `camera-integration/x11-xshmattach-async-baderror-crashes-process.md` |
| 85 | ANHNV_2025 / LinuxDeployTool (UX/UI review bước 4.2) | 2026-07-24 | Gotcha | `FlowLayoutPanel.ClientSize.Width = 0` ngay sau khi `FolderBrowserDialog.ShowDialog()` đóng — WinForms layout chưa recalculate xong khi trả về event handler đồng bộ; fix bằng `BeginInvoke(UpdateControlWidths)` thay vì đọc `ClientSize.Width` trực tiếp; dùng `Math.Max(MIN_VALUE, ...)` để đề phòng | `csharp-winforms/winforms-flowlayoutpanel-clientsize-zero-after-modal.md` |
| 85 | ANHNV_2025 / IPGS.Kiosk.Avalonia (icon Cash xám vĩnh viễn sau thanh toán tiền mặt) | 2026-07-23 | Lỗi | ⚠️ HIGH: `MotoCashViewModel.CleanUp()`/`CarCashViewModel.CleanUp()` gọi `CashDevice.Disconnect()` → `_isConnected=false`; `MainViewModel` singleton không gọi lại `Connect()` (guard `IsLoaded`) → icon Cash xám mãi dù thiết bị vật lý vẫn cắm. Fix: bỏ `Disconnect()` khỏi `CleanUp()` — chỉ unsubscribe event, không đóng shared singleton service. Polling loop idle an toàn (null-checked event, không subscriber). | `avalonia/avalonia-cash-device-disconnect-shared-singleton-grey-icon.md` |
| 83 | ANHNV_2025 / iPGSv4 (chạy `scripts/linux-deb/build-deb.sh` cho IPGS.Kiosk.Avalonia qua /gen-build-deb) | 2026-07-23 | Lỗi | HIGH: build `.deb` qua WSL fail 3 lớp độc lập. (1) `ProjectReference` tuyệt đối Windows (`E:\KZTEK\...`) trong `.csproj` không hiểu được bởi dotnet chạy trong WSL/Linux → MSBuild "Skip" project, kéo theo lỗi CS0246/CS0234 thiếu namespace `Kztek.Tool/Object/Api` — fix bằng `dotnet publish` chạy dotnet.exe GỐC TRÊN WINDOWS (nơi path đó hợp lệ), chỉ dùng WSL cho bước cần Linux thật (`dpkg-deb`). (2) Quoting lồng 2 lớp qua `wsl.exe -- bash -lc "<script nhiều dòng có heredoc>"` làm biến đích rỗng phía WSL (`du`/`mkdir` báo lỗi path chứa dấu `"` lạ) — fix bằng tách script ra file `.sh` riêng, gọi `wsl.exe -- bash <file> <args>`. (3) Git Bash/MSYS tự dịch argument `/mnt/e/...` thành path Windows (chèn `C:/Program Files/.../Git` phía trước) khi gọi native exe `wsl.exe` → fix bằng `MSYS_NO_PATHCONV=1` trước lệnh `wsl.exe` | `dotnet-general/wsl-hybrid-build-windows-projectref-msys-pathconv.md` |
| 86 | dungnn / IPGS.Kiosk.Avalonia (user báo "chưa có kết nối SQL/parking/config thì bấm vào màn config rất lag") | 2026-07-23 | Lỗi | HIGH: `ConnectionConfigWindow` constructor gọi `_vm.Load()` đồng bộ trên UI thread TRƯỚC `ShowDialog`; `Load()` gọi `KioskServices.Db.FillData` đọc timer, và `KioskDbService.OpenInternal()` không set `Connect Timeout` (mặc định SqlClient ~15s) + retry 2 lần + `Thread.Sleep(1000)` → treo UI tới ~30s khi SQL không kết nối được. `Save()` cũng cùng pattern (ghi DB đồng bộ trước khi đóng form). Fix: (1) thêm `Connect Timeout=3` vào connection string (worst-case ~7s thay vì ~30s); (2) tách `Load()` thành JSON-load đồng bộ (nhanh) + `LoadTimerFromDbAsync()` chạy `Task.Run` cập nhật qua `Dispatcher.UIThread.InvokeAsync`, gọi fire-and-forget sau khi form đã hiển thị; (3) `Save()` đóng form (Confirmed+CloseRequested) TRƯỚC, ghi timer DB trong `Task.Run` sau. Build PASS 0 Error | `avalonia/avalonia-config-window-blocks-ui-thread-sql-timeout.md` |
| 87 | dungnn / IPGS.Kiosk.Avalonia (user báo "Connect và DisableAcceptor đều là `_ =` => không có tác dụng"; sau fix v1 user báo "vẫn chưa disable được") | 2026-07-24 | Lỗi | CRITICAL: `DisableAcceptor()` "không có tác dụng" — 2 nguyên nhân xếp chồng, phải sửa CẢ HAI. (1) Nguyên nhân chính, lặp mỗi ~100ms: `DoPollOnceCore_FullSwitchAsOld()` trong `CBA9ControllersV3.cs` (thư viện ngoài `ParkingV8.Cash`, repo `parking-v8-app-avalonia`) check thô `if (response.ToString().Contains("E8")) EnableValidator();` — `0xE8 = SSP_POLL_DISABLED` chính là trạng thái ĐÚNG NHƯ app vừa yêu cầu, nhưng code lại tự bật lại mỗi chu kỳ poll, khiến Disable không bao giờ giữ được quá 100ms. (2) Nguyên nhân phụ, theo sự kiện reconnect: `TryConnectCore()` STEP10 luôn tự `EnableValidatorCore()` cuối MỌI lần (re)connect. Hỏi user chọn sửa tận gốc ở thư viện (ảnh hưởng mọi app dùng chung) hay chỉ workaround trong iPGSv4 — user chọn sửa tận gốc. Fix: thêm field ý định `_acceptorShouldBeEnabled` NGAY TRONG `CBA9ControllersV3` (set trong Enable/DisableValidatorCore, guard thêm vào điều kiện auto-enable E8) + giữ fix wrapper `CashDeviceServiceImpl` (tái áp đặt qua `ConnectionStatusEvent`) làm lớp phòng vệ thứ 2 cho case reconnect. Build PASS 0 Error cả `ParkingV8.Cash.csproj` và `IPGS.Kiosk.Avalonia.csproj`. Bài học: "vẫn chưa được" sau 1 lần fix → tìm nguyên nhân THỨ HAI hoạt động ở tần suất khác, đừng nghi ngờ fix đầu sai | `avalonia/avalonia-cash-acceptor-silently-reenabled-on-reconnect.md` |
| 88 | dungnn / LinuxDeployTool (user báo "tôi đã chọn nhưng sau đó không thấy gì" khi bấm Thêm solution) | 2026-07-24 | Lỗi | HIGH: `SolutionItemControl` (UserControl) add runtime vào `FlowLayoutPanel` nhưng KHÔNG hiển thị — không exception, item có trong `Controls`/list nhưng invisible. Root cause: Designer set `Anchor = Top|Left|Right` trên CHÍNH UserControl gốc (không phải control con bên trong) — WinForms cache khoảng-cách-tới-cạnh-phải lúc `Parent=null`, khi `MainForm.AddItemControl()` set `ctrl.Width` rồi mới `flowPanel.Controls.Add(ctrl)`, re-anchor theo parent mới tính sai → control co về ~0 kích thước. Fix: bỏ `Right` khỏi Anchor của UserControl gốc, chỉ giữ `Top | Left` (Anchor trên control con bên trong UserControl để chúng giãn theo cha vẫn giữ nguyên, đúng). Bài học: control add đúng logic (count tăng) nhưng không hiển thị, không exception → nghi Anchor/Size=0 trước khi nghi Visible/z-order. | `csharp-winforms/winforms-usercontrol-anchor-right-invisible-in-flowlayoutpanel.md` |
| 89 | dungnn / LinuxDeployTool (user test publish thật với solution iPGSv4, log báo "cd: /e/KZTEK/.../iPGSv4: No such file or directory" dù thư mục thật sự tồn tại) | 2026-07-24 | Lỗi | HIGH: `BashRunner.FindGitBash()` fallback quét PATH tìm `bash.exe` trúng nhầm `%LOCALAPPDATA%\Microsoft\WindowsApps\bash.exe` — trên máy có bật WSL, file này là SYMLINK trỏ `wsl.exe`, không phải Git Bash/MSYS thật. Xảy ra vì Git for Windows cài ở path tùy chỉnh (`C:\Program Files\SetUp\Git\`) và chỉ có `<GitRoot>\cmd` (chứa `git.exe`) nằm trong PATH, không có `<GitRoot>\bin`/`usr\bin` (chứa `bash.exe` thật) — 2 hardcoded path chuẩn không khớp, fallback PATH-scan tìm thấy WindowsApps trước. Path convert kiểu MSYS (`/e/...`) đúng, nhưng chạy nhầm qua `wsl.exe` nên fail vì WSL cần `/mnt/e/...`. Fix: thêm `FindGitBashViaGitExeLocation()` — derive từ vị trí `git.exe` (luôn có trong PATH), dò lên 3 cấp thư mục cha tìm `bin\bash.exe`/`usr\bin\bash.exe`; fallback PATH-scan cuối cùng loại trừ mọi path chứa "WindowsApps". Thêm 3 unit test cố định (bao gồm test path trả về không chứa "WindowsApps"). Build PASS 0 Error, 9/9 test pass. | `dotnet-general/git-bash-detection-windowsapps-wsl-symlink-stub.md` |
| 90 | dungnn / iPGSv4 (ApplicationConfig, user báo "khi chưa có config, config sai không kết nối được, không mở được app config => cần sửa lại") | 2026-07-24 | Lỗi | HIGH: tool `ApplicationConfig` (sửa config SQL/LPR/Parking) tự crash toàn bộ process khi mở nếu file `sql.txt` tồn tại nhưng field `Password` không phải Base64 hợp lệ (config bị sửa tay sai/hỏng) — `SqlConfigView.OnLoaded` gọi `SQLConfig.GetDecodePassword(config.Password)` không có try/catch, `Convert.FromBase64String` trong `CryptorEngine.Decrypt` ném `FormatException` không ai bắt, Avalonia không tự cô lập exception trong `Loaded` handler nên crash cả app — chặn đúng con đường user cần dùng để SỬA lại config khi nó hỏng (chicken-egg bug). Case "chưa có config" (file không tồn tại) đã an toàn sẵn vì `NewtonSoftHelper.DeserializeObjectFromPath` trả `null`, không throw. Fix: bọc try/catch riêng đúng dòng decode password, fallback về `""` + hiển thị cảnh báo `⚠ Mật khẩu đã lưu bị lỗi định dạng` trên UI thay vì để crash; các field khác (ServerName/Authentication/DatabaseName) vẫn load bình thường. Build PASS 0 Error | `avalonia/avalonia-config-tool-crash-on-corrupted-password.md` |
| 91 | dungnn / iPGSv4 (IPGSUseCam, user gửi screenshot "Không được" — LoadingWindow báo lỗi SQL chỉ có nút Đóng) | 2026-07-24 | Lỗi | HIGH: sau fix #90, user vẫn kẹt vì `LoadingWindow.axaml` (app chính, không phải tool config) khi lỗi kết nối SQL/Parking/LPR chỉ hiện nút "Đóng" — không có đường nào mở `ApplicationConfig` để sửa config, phải thoát hẳn app. Đây là UX gap thiết kế, không phải crash. Root cause thuận lợi: `IPGSUseCam.csproj` đã sẵn `ProjectReference` tới `ApplicationConfig.csproj`, và `ConfigPathManagement.baseBath` (static, dùng chung path config) đã được `Program.cs` set 1 lần — nên có thể `new ApplicationConfig.Views.MainWindow()` ngay trong CÙNG process, không cần `Process.Start` file .exe riêng (tránh 2 tiến trình tranh chấp file config + dò đường dẫn exe). Fix: thêm nút "Mở Cấu Hình" trong footer `LoadingWindow.axaml` (ẩn mặc định cùng "Đóng"), `ShowError()` hiện cả 2 nút; click → `ShowDialog(this)` mở MainWindow config, đóng xong thì reset UI (ẩn error, hiện lại progress) và tự gọi lại `RunLoadingTasksAsync()` để retry, không bắt user khởi động lại app. Build PASS 0 Error. Bài học: "user báo lỗi X đã sửa" nhưng vẫn báo "không được" kèm screenshot → đối chiếu ĐÚNG màn hình trong ảnh trước khi tin fix trước đã đủ, đừng chỉ suy luận từ code | `avalonia/avalonia-loading-window-no-path-to-fix-config.md` |
| 92 | ANHNV_2025 / iPGSv4 (user yêu cầu chạy `scripts/linux-deb/build-deb.sh` cho IPGSUseCam qua WSL) | 2026-07-24 | Lỗi | HIGH: build `.deb` qua WSL fail vì 4 file `.csproj` (`IPGS.Control/IPGS.Controls.csproj`, `IPGSUseCam/IPGSUseCam.csproj`, `ApplicationConfig/ApplicationConfig.csproj`, `0.BaseLIB/Kztek.Camera/.../Kztek.Cameras.Avalonia.csproj`) dùng `ProjectReference` tuyệt đối Windows (`E:\KZTEK\Code_Git\5.BaseUI\KztekComponentAvalonia\...`, cố ý thêm ngày 2026-07-23) — dotnet chạy trong WSL/Linux không hiểu `E:\...` là ổ đĩa tuyệt đối, MSBuild nối nhầm thành path con project hiện tại → restore "Skip" project trong im lặng → CS0246 hàng loạt (`KzButton`, `KzCheckedListBox`, `KztekComponentAvalonia`...) ở mọi project downstream, dễ nhầm lỗi package/XAML. Xin user xác nhận trước khi sửa (AskUserQuestion) vì có lesson cũ #83 cảnh báo không nên đổi absolute→relative nếu nhiều máy dev có cấu trúc khác nhau. User chọn sửa path tương đối. Đổi cả 4 file sang path tương đối trỏ đúng `Code_Git\5.BaseUI\KztekComponentAvalonia\...` (đếm cấp bằng `cd` + `ls` thực tế, không đếm bằng mắt — đã lệch 1 cấp lần đầu ở file thứ 4, gây lỗi `Skipping project ".../1.Window/5.BaseUI/..."`, sửa lại và build lại). Verify build PASS 0 Error CẢ WSL (dpkg-deb ra `dist/kztek-ipgsusecam_1.0.0_amd64.deb`, 376MB) LẪN Windows (`dotnet build IPGS.Control/IPGS.Controls.csproj`) trước khi coi là an toàn — khác lesson #83 (nhiều máy dev, không sửa path) vì đây chỉ 1 máy dev duy nhất dùng chung `Code_Git\5.BaseUI`. | `dotnet-general/csproj-absolute-projectref-fixed-relative-wsl-build.md` |
| 93 | kienpt / 6.RemoteControlTool (user báo shortcut Ctrl+C/Ctrl+V không hoạt động và click vào Remote Control window bị cả 2 bên máy cùng nhận) | 2026-07-24 | Lỗi | HIGH: 2 bug độc lập trong `RemoteScreenControl`/`KeyboardMapper` (CcuUI). (1) `KeyboardMapper.Resolve` Tier 2 dùng thẳng `KeyEventArgs.KeySymbol` làm keysym X11, nhưng khi giữ Ctrl, Windows/Avalonia trả `KeySymbol` là ASCII control-char (Ctrl+V=0x16) chứ KHÔNG phải chữ cái gốc ('v'=0x76) → ZcuAgent `XKeysymToKeycode` không tìm được physical key cho control-char, skip → Ctrl+C/Ctrl+V không tới ZCU; fix: thêm tham số `KeyModifiers`, khi Control đang giữ thì bỏ qua Tier 2, dùng thẳng Tier 3 (fallback theo `Key` enum → chữ ASCII thường, đúng ý đồ thiết kế ban đầu đã có sẵn docstring nhưng bị Tier 2 che mất). (2) `OnPointerPressed/Released/Moved` trong `RemoteScreenControl.axaml.cs` không set `e.Handled = true` → sự kiện bubble tiếp lên Window/parent, máy CCU cục bộ cũng xử lý click y như UI thường ngoài việc forward tới ZCU; fix set `e.Handled = true` cho cả 3 handler, gộp `Focus()` vào thẳng `OnPointerPressed` (bỏ override `OnPointerPressed(PointerPressedEventArgs)` cũ vì sẽ không còn được gọi sau khi Handled=true). Build PASS 0 Error. | `avalonia/avalonia-keysymbol-ctrl-modifier-control-char.md` |
| 94 | kienpt / LinuxDeployTool (user báo Publish báo lỗi `dpkg-deb: command not found`) | 2026-07-25 | Lỗi | HIGH: fix trước đó (#89, commit `94d0c1f`) loại trừ hẳn WSL khỏi `BashRunner.FindGitBash()` để tránh detect nhầm WSL-stub `bash.exe`, nhưng vô tình khiến `dpkg-deb` (chỉ tồn tại trong Linux/WSL thật, KHÔNG BAO GIỜ có trong Git Bash/MSYS for Windows dù detect đúng bash.exe) không bao giờ chạy được — mọi lần Publish fail ở bước cuối `[7/7] Đóng gói .deb`. Fix: đảo ngược quyết định trước, `BashRunner` chuyển hẳn sang chạy qua `wsl.exe` thật (`FindWsl()`/`ToWslPath()` → `/mnt/<ổ>/...`, không phải format MSYS `/e/...`); phát sinh 2 gotcha phụ trong lúc verify: (a) WSL thiếu dotnet SDK trong PATH non-interactive (`wsl.exe -e bash -c` không load `.bashrc`) — cần symlink `/usr/local/bin/dotnet`; (b) race condition tạm thời `CSC: Metadata file .../ref/Xxx.dll could not be found` khi build ProjectReference qua DrvFs, build lại lần 2 qua. Verify build `.deb` thành công end-to-end (iPGSv4/IPGSUseCam, 376MB). 8/8 unit test pass. Bài học: khi loại trừ 1 thứ để fix detection bug, phải hỏi "code phía sau có PHỤ THUỘC cứng vào đúng cái vừa loại trừ không?" trước khi merge. | `dotnet-general/wsl-required-for-linux-native-tools-not-git-bash.md` |
| 95 | kienpt / LocationAndPayment v8 (IPGS.Kiosk.Avalonia, user báo publish qua LinuxDeployTool vẫn lỗi sau fix #94) | 2026-07-25 | Lỗi | HIGH: fix #94 (BashRunner → WSL toàn bộ) làm lộ ra pattern tái diễn y hệt lesson #83/#92 — `IPGS.Kiosk.Avalonia.csproj` có 8 dòng `ProjectReference` tuyệt đối Windows (`E:\KZTEK\...`) theo ADR §8.2 (2 repo song song), dotnet publish trong WSL không hiểu path này → CS0246 hàng loạt thiếu `Kztek`/`iParkingv8`. Khác với case #92: 8 dòng KHÔNG cùng số cấp `../` (6 dòng sang `parking-v8-app-avalonia` cần 8 cấp, 2 dòng sang `5.BaseUI` cần 9 cấp) — dùng `realpath --relative-to` tính chính xác từng dòng thay vì giả định đồng nhất. User xác nhận rõ ràng 2 lần (từng gõ nhầm "path tuyệt đối" giữa chừng, phải AskUserQuestion lại để chắc chắn) trước khi sửa — không tự suy luận ý user khi có tín hiệu mâu thuẫn. Verify build Windows (`dotnet build -c Release`) PASS 0 Error + build-deb.sh qua WSL PASS (`dist/kztek-ipgskioskavalonia_1.0.5_amd64.deb`). Đồng thời kiểm tra thêm project CCU (`1.CCU/1.Main/iPGSv4`) — không có ProjectReference tuyệt đối, build qua WSL PASS ngay không cần sửa. Cùng lúc thêm UX: `SolutionItemControl` chỉ hiện dòng log cuối bị cắt ngắn khiến user "không biết lỗi gì" — thêm nút "Xem Log" + `LogViewerForm` hiển thị toàn bộ log tích lũy. | `dotnet-general/csproj-absolute-projectref-fixed-relative-wsl-build.md` (cập nhật, không tạo lesson mới) |
| 96 | kienpt / LinuxDeployTool (user báo log hiển thị lỗi font: "chá»¡y" thay vì "chạy") | 2026-07-25 | Lỗi | HIGH: `Process` mặc định decode stdout/stderr của `wsl.exe` theo `Console.OutputEncoding` (codepage Windows, thường CP1252) nhưng bash/WSL luôn in UTF-8 → mojibake mọi ký tự tiếng Việt có dấu trong log. Fix: set tường minh `psi.StandardOutputEncoding = Encoding.UTF8` và `StandardErrorEncoding = Encoding.UTF8` trong `BashRunner`. Verify: echo tiếng Việt qua `wsl.exe` từ PowerShell — trước fix ra `chá»¡y`, sau fix ra đúng `chạy`. Nguyên tắc chung: bất kỳ `Process` nào capture output từ 1 chương trình biết chắc luôn in UTF-8 (Linux/WSL tool) trên Windows PHẢI set StandardOutputEncoding/StandardErrorEncoding tường minh, không dựa vào default. | `.claude/GOTCHAS.md` G002 (LinuxDeployTool) |
| 97 | kienpt / IPGS.Kiosk.Avalonia + IPGSUseCam (user báo "bản build .deb mới thiếu file .so ffmpeg của Kztek.Cameras.Avalonia") | 2026-07-25 | Lỗi | HIGH: điều tra ban đầu tưởng file .so bị thiếu khỏi output publish (do path tuyệt đối bị Exists() fail dưới WSL, giống lesson #95) — nhưng verify thực tế (rm -rf + rebuild sạch qua WSL) cho thấy file .so VẪN được copy đúng vào `Players/FFMPEG/Resource/`. User tự xác nhận đúng hướng: "có thư viện .so ffmpeg trong thư mục resource rồi, cần sửa lại file build-det set lại đúng biến môi trường" + "không nhận biến môi trường => không xem được video". Nguyên nhân thật: `FFmpegLoader.cs` nạp lib FFmpeg đầu tiên bằng `NativeLibrary.Load(fullpath)` (không cần LD_LIBRARY_PATH), nhưng các .so này phụ thuộc lẫn nhau (libavformat.so cần libavcodec.so/libavutil.so...) — dependency NỘI BỘ này do `ld.so` của Linux resolve, CHỈ tìm theo LD_LIBRARY_PATH/RPATH/system path, không tự biết thư mục vừa nạp lib đầu. `run.sh` chỉ export `LD_LIBRARY_PATH="$DIR"`, thiếu `$DIR/Players/FFMPEG/Resource`. Verify bằng `ldd libavformat.so`: thiếu biến → `libavcodec.so.62 => not found`; thêm biến → sạch. Bug lặp y hệt ở CẢ 2 project dùng chung thư viện camera (`IPGS.Kiosk.Avalonia`, `IPGSUseCam`) — mỗi project có `run.sh` riêng, phải sửa cả 2. Verify cuối: giải nén `.deb` mới, `run.sh` bên trong đã có dòng fix. Bài học: đừng tin giả thuyết ban đầu (dù logic hợp lý) khi chưa verify thực tế — user cung cấp thêm 1 câu mô tả đúng (env var) giúp chuyển hướng điều tra nhanh hơn nhiều so với tự suy luận tiếp. | `camera-integration/ffmpeg-linux-nativelibrary-load-fullpath-still-needs-ld-library-path.md` |
| 98 | kienpt / parking-v8-app (IPARKING v8, user báo ảnh chụp thực tế dialog xác nhận xe ra bị cắt: mất nút X, mất hàng nút bấm) | 2026-07-25 | Lỗi | CRITICAL: dialog `UcConfirmOut` hiển thị thiếu nội dung — mất nút đóng "X", hàng nút (Xác nhận/Voucher/Đổi thẻ) chỉ còn sliver, nhãn cột phải bị cắt chữ ("Nhóm định danh" → "Nhóm định d."), tiêu đề `lblMessage` mất nửa phải và lệch phải; nửa trên (ảnh camera) vẫn bình thường. "Thỉnh thoảng mới bị", máy chuẩn 1920x1080 @ scale 100% nên KHÔNG phải lỗi DPI. Đã điều tra sai hướng qua 5 giả thuyết (maskedForm nhỏ → co dialogHost; MinimumSize=672 < nhu cầu 758px; flowLayoutPanel1 wrap do 5 nút = 582/583px; tableLayoutPanel AutoSize tràn; region bo góc Guna2Elipse clip) — tất cả bị chính user phản biện đúng: overlay vẫn phủ đủ, và `Dock=Fill` thì Parent/Child size phải khớp. Root cause thật: dòng 561 gõ `this.SuspendLayout()` vào đúng chỗ đáng ra là `this.ResumeLayout(false)` (2 dòng dưới nó là `dialogHost.ResumeLayout`/`maskedForm.ResumeLayout`) → suspend count của UserControl = +2 MỖI lần mở dialog, không bao giờ về 0 → layout engine của UserControl treo vĩnh viễn. Vì UserControl là con của `dialogHost` (engine cha khoẻ) nên `this.Size` VẪN khớp `dialogHost.Size` (đúng như user lập luận), nhưng control BÊN TRONG UserControl (`btnCancel` Anchor Top\|Right ở X=551, `tableLayoutPanel1` 583x694, `lblMessage` W=541) do engine của chính UserControl sắp đặt → `Anchor` không bao giờ tính lại, giữ toạ độ thiết kế 599x768 → bị clip mép phải + mép dưới. "Thỉnh thoảng" = lần mở đầu tiên vẫn đúng (count=0 lúc resize), từ lần 2 trở đi mới hỏng. Fix: `this.ResumeLayout(true)` (true để buộc layout lại vì Visible/Height đã đổi trong lúc treo) + thêm `finally` resume cả 3 đối tượng (các SuspendLayout đầu hàm nằm NGOÀI try ở dòng 515) + log chẩn đoán tạm. Verify: `UcConfirmIn.cs` cân bằng đúng 2↔2 nên CHỈ dialog xe RA bị lỗi (khớp 100% mọi ảnh user gửi). Build PASS 0 Error. Bài học: khi Anchor "không phản ứng" mà không có exception → đếm SuspendLayout/ResumeLayout theo TỪNG đối tượng trước khi nghi DPI/MinimumSize/region; dấu hiệu vàng là Parent.Size==Child.Size khớp đúng nhưng các cháu bên trong lệch. | `csharp-winforms/winforms-suspendlayout-unbalanced-anchor-not-recalculated.md` |
| 99 | kienpt / claude (repo config KZTEK — chuyển config sang user-level scope dùng chung mọi project) | 2026-07-25 | Lỗi | HIGH: viết `scripts/link-global.ps1` (tạo junction đưa `.claude/` lên `~/.claude` cho mọi project dùng chung) — chạy lần đầu báo hàng loạt lỗi parser vô nghĩa `Missing '=' operator after key in hash literal`, `The string is missing the terminator: "`, `Missing closing '}'` trỏ vào những dòng cú pháp hoàn toàn đúng. Nguyên nhân thật: Windows PowerShell 5.1 mặc định đọc file `.ps1` KHÔNG có BOM theo codepage ANSI (Windows-1252), không phải UTF-8 — tool `Write` của Claude Code ghi UTF-8 không BOM nên key hashtable tiếng Việt `Trạng_thái` bị giải mã thành `Tráº¡ng_thÃ¡i`, ký tự rác phá vỡ token của parser. Dấu hiệu vàng để nhận ra: nhìn dòng echo lại trong chính thông báo lỗi, thấy mojibake (`Ã`, `áº`) là xác nhận encoding, KHÔNG phải cú pháp. Fix cả 2 mặt: (1) không dùng chữ Việt trong identifier — đổi key thành `Status`, chữ Việt chỉ để ở comment/chuỗi hiển thị; (2) thêm UTF-8 BOM vào file (`b'\xef\xbb\xbf' + raw` bằng Python; hoặc `Out-File -Encoding utf8`) để comment/chuỗi tiếng Việt vẫn hiển thị đúng khi chạy. Lưu ý `Set-Content`/`Add-Content` của 5.1 mặc định ghi ANSI nên dùng để "sửa lại" sẽ làm hỏng thêm. PowerShell 7+ không bị lỗi này (mặc định UTF-8) → script cần chạy trên Windows mặc định PHẢI test bằng `powershell.exe` 5.1. Bài học: lỗi parser PowerShell trông như thiếu ngoặc/dấu bằng mà cú pháp đúng → kiểm tra encoding TRƯỚC, đừng soát cú pháp. | `windows-tooling/powershell-vietnamese-bom-parse-error.md` (category mới) + `.claude/shared/GOTCHAS.md` G002 |
| 100 | vietanh / iPGSv4 CCU (rà soát mức độ hoàn tất chuyển đổi Avalonia) | 2026-07-26 | Gotcha | HIGH: migration được báo cáo "hoàn tất, 0 errors, sign-off production" nhưng rà soát lại phát hiện 3 module bị `<Compile Remove>` khỏi csproj mà source vẫn nằm trong repo nên nhìn qua tưởng còn đủ: tầng camera Dahua/Hik, REST API `Controller/APIController.cs` (38 KB), giao thức Kocom `KOCOM/KocomHelper.cs` (22 KB). Tệ nhất là code gọi vào tầng đã gỡ bị vá thành no-op im lặng (`if (cameraController == null) { cameraController = null; }`) → không log, không throw, vận hành chỉ thấy "camera không lên" mà không có manh mối. Dấu hiệu vàng: config vẫn được nạp (`Program.cs` gọi `LoadKocomConfig()`) trong khi consumer đã bị gỡ khỏi build. Đã xử lý: bỏ hẳn tầng camera theo quyết định chủ dự án (xóa 5 file/2544 dòng, dọn `CameraObject` còn thuần entity), xóa toàn bộ source WinForms chết + gỡ sạch `<Compile Remove>` thừa trong 3 csproj, thu hồi sign-off sai. Bài học: "0 errors" KHÔNG phải bằng chứng migration đủ — phải grep `Compile Remove` và phân loại từng mục là UI framework cũ (xóa được) hay logic nghiệp vụ (chưa migrate). | `avalonia/migration-compile-remove-hides-unmigrated-code.md` |
| 101 | vietanh / iPGSv4 CCU (dọn 142 warning AVLN5001 sau nâng Avalonia 12.1.0) | 2026-07-26 | Gotcha | MEDIUM: Avalonia 12 đánh obsolete `TextBox.Watermark` (→ `PlaceholderText`) và `Window.SystemDecorations` (→ `WindowDecorations`, giá trị enum giữ nguyên). Build vẫn xanh nên dễ bỏ qua, nhưng 142 lượt warning lấp kín output che mất warning thật. Thay hàng loạt bằng regex trên `.axaml` — NHƯNG bẫy: `KzPasswordTextBox` của KztekComponentAvalonia tự khai báo `StyledProperty` tên `Watermark` là API công khai của thư viện dùng chung, thay mù sẽ phá mọi project consumer; chỉ đổi chỗ nó gán xuống TextBox nội bộ (`tb.PlaceholderText = Watermark`), giữ nguyên tên property ngoài. Kiểm tra trước khi thay: control `: TextBox` (kế thừa → thay được) hay TemplatedControl tự định nghĩa `Watermark` (giữ nguyên). Lưu ý đếm warning: grep trên output `-v n` bị GẤP ĐÔI số thật (MSBuild in cả ở pass build lẫn summary) — lấy dòng `N Warning(s)` của summary. Kết quả: AVLN5001 về 0, build 0 errors / 950 warnings. | `avalonia/avalonia-12-watermark-systemdecorations-obsolete.md` |
| 102 | vietanh / iPGSv4 IPGSUseCam (cải tiến Avalonia — bước 1.1 của PLAN-improve-ipgsusecam) | 2026-07-26 14:30 | Gotcha | MEDIUM: `dotnet build` solution Avalonia nhiều project thất bại NGẪU NHIÊN với `AVLN9999: The process cannot access ApplicationConfig.dll` hoặc `CS0006: Metadata file .../ref/KztekComponentAvalonia.dll could not be found` — không sửa gì, build lại có khi xanh, rất dễ tưởng lỗi do code vừa viết và debug nhầm hướng. Nguyên nhân: MSBuild build song song mặc định, còn Avalonia XAML compiler (`Avalonia.Build.Tasks`) mở/ghi đè assembly output SAU khi Roslyn emit; hai project cùng chạm output/ref-assembly của một dependency chung tại cùng thời điểm → file lock. `CS0006` là hệ quả dây chuyền (project dependency bị abort nên ref assembly chưa kịp sinh). Fix: `dotnet build-server shutdown` (chỉ khi đã dính) rồi luôn build/test với `-m:1`. KHÔNG cần xoá `obj/`, `dotnet clean`, hay kiểm tra app có đang chạy (đã xác nhận không process nào mở). Bài học: với solution Avalonia nhiều project chia sẻ dependency, đặt `-m:1` làm mặc định trong mọi script build/test tự động. | `avalonia/avalonia-parallel-build-file-lock.md` + `.claude/GOTCHAS.md` G011 (iPGSv4) |
| 103 | vietanh / iPGSv4 (buoc 2.1 PLAN-cai-tien-ipgs-kiosk-avalonia) | 2026-07-26 15:05 | Gotcha | MEDIUM: Tao solution file dau tien cho IPGS.Kiosk.Avalonia thi dinh 2 bay lien tiep. (1) `dotnet build App.sln -c Release -r win-x64` loi `NETSDK1134: Building a solution with a specific RuntimeIdentifier is not supported` - RuntimeIdentifier la thuoc tinh cap PROJECT, solution co the chua nhieu project khac TFM/RID nen SDK chan thang thay vi doan; day la hanh vi CO CHU DICH, khong phai loi cau hinh. Tach ro: build toan solution thi KHONG `-r`, build/publish theo RID thi chay o cap csproj. (2) `dotnet new sln` tren SDK 10 bao "created successfully" nhung sinh ra `.slnx` (dinh dang XML moi) chu khong phai `.sln`, nen lenh `dotnet sln Ten.sln add` ke tiep bao "Could not find solution" - dung `--format sln` neu can dinh dang co dien. Bat ngo them: `dotnet sln add A.csproj` TU DONG keo theo toan bo project ma A tham chieu (them 1 project ra solution 10 project) - kiem lai bang `dotnet sln list`. | `dotnet-general/dotnet-sln-rid-build-va-slnx-mac-dinh.md` |
| 104 | vietanh / RemoteControlTool (bước 5.1 PLAN-remote-control-audit-fix) | 2026-07-26 15:05 | Lỗi | HIGH: TCP server ZcuAgent — heartbeat/watchdog đặt CÙNG loop với `WriteAsync` không timeout → client slow-reader chặn write vô hạn, timeout 15s không bao giờ fire, 1 client kẹt chiếm luôn slot session duy nhất (DoS). Kèm bẫy: `NetworkStream.WriteTimeout` CHỈ áp dụng cho sync Write, bị bỏ qua hoàn toàn với WriteAsync. Fix: tách watchdog PONG thành task riêng (Interlocked `_lastPongTicks`) + mọi WriteAsync bọc CTS `CancelAfter(10s)` convert timeout → IOException, phân biệt với session-cancel qua `when (!ct.IsCancellationRequested)` | `networking-protocol/tcp-async-write-timeout-watchdog-separate-task.md` |
| 105 | vietanh / RemoteControlTool (bước 5.1 PLAN-remote-control-audit-fix) | 2026-07-26 15:05 | Pattern | HIGH: DPAPI `ProtectedData` chỉ chạy Windows — app cross-platform (win-x64;linux-x64) mã hoá secret phải xử lý Linux tường minh (cảnh báo NỔI BẬT 1 lần qua Interlocked guard), TUYỆT ĐỐI không im lặng fallback plaintext. Pattern tái dùng: prefix version `enc:v1:` phân biệt ciphertext/plaintext cũ → migrate lazy (đọc plaintext trả nguyên, tự mã hoá ở lần Persist kế trên bản clone); mọi decrypt fail trả rỗng + log (không throw — không mất cả danh sách profile); không bao giờ log giá trị secret | `dotnet-general/dpapi-protecteddata-cross-platform-secret-migration.md` |
| 106 | vietanh / RemoteControlTool (bước 5.1 PLAN-remote-control-audit-fix) | 2026-07-26 15:05 | Gotcha | HIGH: bộ 3 gotcha chạy lệnh shell từ .NET qua SSH/Process: (1) whitelist filename phải cho phép `~`/`%` trừ ký tự đầu — version chuẩn Debian chứa `~` (`pkg_1.0~rc1_amd64.deb`), whitelist "thông thường" từ chối oan; (2) `printf '%s\n' '<arg>'` an toàn tuyệt đối với `%` trong argument (format string là hằng) — chỉ nguy hiểm khi đặt chuỗi user vào format string; xóa crontab cuối → `crontab -r`; (3) `xclip` tự daemonize giữ X selection → WaitForExit luôn timeout, phải dispose Process ngay và KHÔNG kill process tree (mất clipboard) | `dotnet-general/ssh-shell-quote-whitelist-printf-xclip-gotchas.md` |
| 107 | vietanh / DoorAlarm v3 (review code toàn project) | 2026-07-26 15:14 | Lỗi | HIGH: `Kztek.Cameras.Camera.Start()` ở repo dùng chung `0.BaseLIB` đã đổi chữ ký (bỏ `bool enableMotionDetection` + `int cameraSDK`, tách `motionAlarmLevel` thành `from/to`, thêm `EmVirtualLoopType`) trong lúc DoorAlarm v3 không được build lại → project "ngủ đông" ở trạng thái KHÔNG BUILD ĐƯỢC (7 lỗi CS1503 lệch so le từ tham số 1 đến 7). Vì tham số toàn primitive nên compiler không cảnh báo được lúc sửa thư viện. Nhận dạng: chuỗi CS1503 lệch nhiều tham số liên tiếp = đổi chữ ký, không phải sai 1 tham số. Cách lấy chữ ký cũ: `git log -S"public void Start(bool" -- Camera.cs`; cách lấy call chuẩn: grep project đã migrate (ParkingV8 dùng `Start(0, 0, 0)` cho liveview). Bài học vận hành: LUÔN `dotnet build` TRƯỚC khi đọc/sửa project có ProjectReference sang `0.BaseLIB`; dùng named argument cho tham số optional để lần drift sau fail-fast tại compile thay vì bind nhầm vị trí. | `camera-integration/kztek-cameras-start-signature-drift.md` |
| 108 | vietanh / DoorAlarm v3 (review code toàn project) | 2026-07-26 15:14 | Lỗi | CRITICAL: driver `KZE02` chạy polling bằng `new Thread(new ThreadStart(WorkerThread))` với `WorkerThread` là `async void` → thread vật lý kết thúc NGAY tại `await` đầu tiên, phần còn lại chạy trên thread pool. Dây chuyền: `Running` (dựa `thread.Join(0)`) luôn false → `PollingStop()` bọc trong `if (Running)` không bao giờ chạy → mỗi lần mở/đóng màn Cài đặt lại tạo thêm 1 vòng polling UDP tới cùng thiết bị (cộng dồn), kèm `ObjectDisposedException` ngẫu nhiên do `Free()` Close `ManualResetEvent` khi vòng lặp còn WaitOne. Không có exception nào lộ ra — chỉ thấy thiết bị phản hồi chậm dần, mất event. Fix: `Task` + `CancellationTokenSource`, `PollingStart` idempotent, `PollingStop` phải await vòng cũ thoát thật rồi mới Dispose CTS, bỏ `Application.DoEvents()`. Kèm bẫy phụ: `lock(list) { return list; }` không thread-safe — phải trả bản sao. | `csharp-winforms/thread-async-void-workerthread-pollingstop-vo-hieu.md` |
| 109 | vietanh / App-Access-V2 (STEP-0.3 PLAN-migrate-avalonia) | 2026-07-26 15:35 | Lỗi | HIGH: `.git/refs/heads/dev` hỏng thành đúng 41 byte NUL sau khi tiến trình bị ngắt giữa lúc ghi ref (metadata cấp phát xong, data chưa flush — NTFS) → mọi lệnh git báo `fatal: your current branch appears to be broken`, `git rev-parse HEAD` fail, tưởng mất commit vừa tạo. Thực tế commit object nằm trong `.git/objects/` HOÀN TOÀN độc lập với file ref 41 byte ⇒ không mất gì. Khôi phục: lấy SHA cột 2 dòng cuối của FILE `.git/logs/refs/heads/<branch>` (không dùng lệnh `git reflog` — lệnh này cũng phải resolve ref hỏng) → `git cat-file -t <sha>` xác minh còn sống → `rm -f .git/refs/heads/<branch>` (BẮT BUỘC: `update-ref` báo `cannot lock ref ... reference broken` nếu file hỏng còn đó) → `git update-ref refs/heads/<branch> <sha>` → verify `git fsck --connectivity-only`. Bẫy phụ: `$(cat .git/refs/heads/x)` bị bash nuốt byte NUL trả chuỗi rỗng làm lỗi hiển thị sai lệch | `dotnet-general/git-ref-file-corrupted-null-bytes-recovery.md` |
| 110 | vietanh / iPGSv4 Kiosk NGANG (nhóm D3c PLAN-migrate-kiosk-horizontal-avalonia) | 2026-07-26 16:05 | Gotcha | MEDIUM: `ItemsControl` + `ItemsPanel=Canvas` không định vị được item theo toạ độ — 2 nguyên nhân chồng nhau: (1) con TRỰC TIẾP của Canvas là `ContentPresenter` (container) chứ không phải gốc `ItemTemplate`, nên `Canvas.Left/Top` đặt trong template bị bỏ qua — build SẠCH, không warning, chỉ thấy mọi item đè nhau tại (0,0); (2) chuyển sang đặt qua `Style Selector="ItemsControl > ContentPresenter"` thì compiled binding resolve theo `x:DataType` của View (ViewModel) chứ không phải kiểu item → `AVLN2000 Unable to resolve property 'X'`. Fix: đổi `ItemsPanel` sang `Grid`, thêm property `Thickness Offset => new(X, Y, 0, 0)` vào model item, template dùng `Margin="{Binding Offset}"` + BẮT BUỘC `HorizontalAlignment=Left` & `VerticalAlignment=Top` (thiếu 2 cái này item stretch full ô, Margin thành padding — lỗi im lặng). Gọi command VM cha từ item: `$parent[ItemsControl].((vm:T)DataContext).Cmd` | `avalonia/avalonia-itemscontrol-canvas-position-compiled-binding.md` |
| 111 | vietanh / iPGSv4 (seed dữ liệu cho WF-DOCS manual) | 2026-07-26 16:40 | Lỗi | HIGH: `tblLed.GetData()` (IPGS.Object) parse cứng `int.Parse(row[col].ToString())` trên các cột schema khai NULL-able (`zero_color`, `led_function`, `number_of_line`, `type_detail`) — `DBNull.ToString()` trả `""` → `FormatException`. Exception ném trong `LoadingViewModel.LoadLed()` chạy nền, không ai bắt ⇒ app CHẾT IM LẶNG trước cả màn đăng nhập, không popup, tiến trình biến mất. `current_number` còn nguy hơn: `.Split(",").Select(int.Parse)` vỡ với chuỗi rỗng. Seed SQL phải điền đủ `zero_color=1, led_function=0, lines_config='', resolution='64x32', led_row_index=0`. Bài học: `sys.columns.is_nullable` KHÔNG nói lên code chịu được NULL — luôn đọc hàm `GetData()` tương ứng trước khi seed. Bắt lỗi app Avalonia chết im: `Start-Process cmd.exe -ArgumentList "/c","app.exe > out.txt 2>&1"` (chạy `& app.exe` trực tiếp trong PS mất stack trace) | `dotnet-general/tbl-led-getdata-crash-on-null-columns.md` |
| 112 | vietanh / iPGSv4 (seed dữ liệu cho WF-DOCS manual) | 2026-07-26 16:40 | Gotcha | Cập nhật lesson BOM đã có: (1) công cụ `Edit` XOÁ BOM đã thêm trước đó → lỗi parser tiếng Việt tái phát y hệt, phải verify BOM sau MỖI lần Edit chứ không chỉ sau Write đầu tiên; (2) mở rộng sang file `.sql`: cần BOM UTF-8 **và** `sqlcmd -f 65001` — thiếu một trong hai thì dữ liệu vào DB thành `Tầng`; phân biệt rõ với `$env:PYTHONIOENCODING` (chỉ cho Python, không thay thế được) | `windows-tooling/powershell-vietnamese-bom-parse-error.md` |
| 113 | vietanh / RemoteControlTool (hotfix sau audit-fix) | 2026-07-26 17:05 | Lỗi | HIGH: siết bảo mật chuyển password sudo sang STDIN của SSH channel làm MỌI lệnh sudo hỏng với `sudo: no tty present and no askpass program specified`. Nguyên nhân KHÔNG phải quoting hay thứ tự gọi `CreateInputStream()` (thứ tự `ExecuteAsync()` trước là ĐÚNG theo doc SSH.NET) mà là nhánh điều kiện: chỉ rewrite `sudo -S -p ''` khi CÓ password, nhánh else chạy `sudo` TRẦN — mà SSH exec channel không cấp pseudo-tty nên sudo cố mở /dev/tty. Quy tắc: hễ có sudo là LUÔN thêm `-S`, thiếu password thì báo lỗi ngay phía client. Bảng chẩn đoán 3 thông báo sudo: `no tty present` = thiếu `-S`; `no password was provided` = có `-S` nhưng stdin rỗng; `incorrect password attempts` = password sai. Bài học quy trình: build sạch 0 error KHÔNG phải bằng chứng hoạt động với thay đổi đụng xác thực/quyền | `dotnet-general/ssh-shell-quote-whitelist-printf-xclip-gotchas.md` |
| 114 | vietanh / App-Access-V2 (STEP-2.3 PLAN-migrate-avalonia) | 2026-07-26 16:45 | Lỗi | HIGH: Sync-block `IHost.StopAsync().GetAwaiter().GetResult()` trong `desktop.ShutdownRequested` (Avalonia) → deadlock UI thread (dump: `AvaloniaSynchronizationContext.Wait` bên trong `DoShutdown`/WndProc), Kestrel unbind xong (port đóng) nhưng process sống mãi. Fix: pattern cancel-first — `e.Cancel=true` TRƯỚC await đầu tiên, stop host bằng `Task.Run` + `Task.WhenAny(timeout 10s)`, rồi `desktop.Shutdown()` lần 2 với flag chống re-entry. Bẫy kèm theo: process treo giữ lock exe → vòng test sau chạy binary CŨ khiến tưởng fix không ăn — phải Stop-Process trước mỗi rebuild+retest | `avalonia/avalonia-shutdownrequested-sync-wait-ihost-stopasync-deadlock.md` |
| 115 | vietanh / iPGSv4 (WF-DOCS manual Avalonia) | 2026-07-26 17:00 | Gotcha | MEDIUM: Automation chụp màn hình app Avalonia bằng Win32: (1) Enter kích hoạt nút `IsDefault` của Window (thường là Lưu) chứ KHÔNG phải nút đang giữ focus sau khi Tab → form đóng ngoài ý muốn, phải Tab-cuộn control vào view rồi click theo tọa độ; (2) wheel event tổng hợp (mouse_event 0x0800) không cuộn được ScrollViewer — dùng Tab để auto-scroll; (3) đóng cửa sổ tin cậy nhất bằng `PostMessage WM_CLOSE` (không cần foreground, không bị foreground-lock như Alt+F4/SetForegroundWindow); (4) chụp dialog phải bám HWND/tiêu đề riêng vì Avalonia đổi `MainWindowHandle` khi cửa sổ con foreground | `avalonia/avalonia-ui-automation-screenshot-gotchas.md` |
| 103 | vietanh / iPGSv4 IPGSUseCam (sửa .csproj + CODE-GRAPH.md bằng script Python qua Bash tool) | 2026-07-26 17:10 | Gotcha | HIGH: heredoc của Bash tool THU GỌN `\` thành `\` trước khi Python nhận source, nên `'..\5.BaseUI'` tới Python thành `'..\5.BaseUI'` và `\5` bị đọc là escape BÁT PHÂN → ký tự `\5`. Hệ quả lần 1: `str.replace()` chạy xong in 'ok' nhưng file KHÔNG đổi gì (chuỗi tìm kiếm sai âm thầm) — mất thời gian tưởng regex sai. Hệ quả lần 2: ghi Markdown có `$(Root)\5.BaseUI` → file nhiễm ký tự điều khiển `\5`/`\1`/`\0`, `grep` báo `Binary file matches`, converter DOCX chết với `All strings must be XML compatible... no control characters`. Dấu hiệu vàng: replace 'chạy mà không đổi gì' trên chuỗi có `\`, hoặc grep bỗng coi file text là binary. Fix: KHÔNG viết backslash literal trong Python source qua heredoc — dựng bằng `BS = chr(92)` rồi nối chuỗi/regex; hoặc dùng tool Edit/Write cho file có đường dẫn Windows. Luôn verify sau khi ghi: `[hex(b) for b in open(p,'rb').read() if b<0x09 or 0x0b<=b<=0x0c or 0x0e<=b<=0x1f]` phải rỗng. Bài học: script báo thành công vẫn có thể vừa ghi rác vào file — kiểm lại file, đừng tin print('ok'). | `windows-tooling/bash-heredoc-collapses-backslash-in-python.md` |
| 116 | vietanh / DoorAlarmv3.Avalonia (STEP-2.4 PLAN-migrate-dooralarmv3-avalonia) | 2026-07-26 17:09 | Lỗi | HIGH: `MsBox.Avalonia` 3.0.0-rc2 KHÔNG chạy được trên Avalonia 12 — build xanh 0 error nhưng gọi `MessageBoxManager.GetMessageBoxStandard()` là `TypeLoadException: Could not load type 'Avalonia.Controls.SystemDecorations'`. Nguyên nhân: gói biên dịch với Avalonia.Controls 11, .NET phân giải assembly theo TÊN nên nạp bản 12, mà `SystemDecorations` đã bị xoá ở 12. NuGet chỉ có duy nhất 3.0.0-rc2 → không có đường nâng version → gỡ gói, tự viết `MessageDialogWindow` (~110 dòng, theo được token thương hiệu Kz). Cùng dạng lỗi với `Avalonia.Diagnostics` 11 + core 12. Phát hiện nhờ hàm self-test chạy bằng biến môi trường `DA_SELFTEST=1` (dựng mọi service DI + dựng hộp thoại, in PASS/FAIL rồi thoát) — chạy được cả Windows lẫn Linux, bắt lỗi trong vài giây thay vì bấm tay qua UI. Cảnh báo: `Kztek.Cameras.Avalonia` ở 0.BaseLIB vẫn đang khai báo MsBox 3.0.0-rc2 cùng Avalonia 12 → cần kiểm lại | `avalonia/msbox-avalonia-3rc2-incompatible-avalonia-12.md` |
| 111 | vietanh / iPGSv4 Kiosk NGANG (nhóm E PLAN-migrate-kiosk-horizontal-avalonia) | 2026-07-26 17:20 | Gotcha | HIGH: `dotnet build --no-dependencies` (build tăng dần) BỎ QUA hẳn target `CompileAvaloniaXaml` → in `0 Error(s)` dù AXAML đang lỗi `AVLN2100`/`AVLN2000`. Lỗi C# vẫn hiện nên rất dễ tin là sạch. Dấu hiệu nhận biết: chạy LẠI Y NGUYÊN lệnh build mà số lỗi tụt từ N về 0 — đó là XAML compile bị skip do MSBuild coi up-to-date, KHÔNG phải đã sửa xong. Bị lừa 2 lần trong cùng session (2 errors→0, rồi 28 errors→0). Quy tắc: build nhanh chỉ để bắt lỗi C#; muốn kết luận "build sạch" PHẢI `rm -rf obj bin` + build ĐẦY ĐỦ kèm `-m:1` (parallel gây file lock). Bẫy phụ: sau khi xoá obj thì KHÔNG được dùng `--no-dependencies` nữa vì project phụ thuộc chưa có `obj/Debug/net8.0/ref/*.dll` → `CS0006 Metadata file could not be found` (lỗi giả khác). Hệ quả nếu bỏ qua: view lỗi binding build "sạch", chỉ crash lúc navigate tới đúng màn đó → trôi vào bản deploy | `avalonia/avalonia-incremental-build-skips-xaml-compile-false-zero-error.md` |
| 117 | vietanh / App-Access-V2 (STEP-3.2 PLAN-migrate-avalonia) | 2026-07-26 17:20 | Gotcha | HIGH: `KzPasswordTextBox` (KztekComponentAvalonia) là UserControl — `TextProperty` tự đăng ký KHÔNG có `defaultBindingMode: TwoWay` → bind `Text="{Binding X}"` chỉ OneWay: UI hiện giá trị đầu đúng nhưng VM không nhận giá trị user gõ, save ra rỗng, KHÔNG có lỗi nào (silent failure). Kèm 2 bẫy: `Watermark` có default "Nhập mật khẩu" (phải set `""` nếu parity không có placeholder); `CornerRadius`/`FontSize` set ngoài không forward vào PART_Border/PART_Input (`ApplySize()` ghi đè theo `KzSize`). `KzTextBox : TextBox` KHÔNG bị (TwoWay mặc định). Quy tắc: bind property control Kz dạng UserControl → kiểm `AvaloniaProperty.Register` có TwoWay chưa, không có thì viết `Mode=TwoWay` tường minh | `avalonia/kzpasswordtextbox-usercontrol-oneway-text-binding.md` |
| 119 | vietanh / 6.RemoteControlTool (hỗ trợ VM VirtualBox sau mất điện host) | 2026-07-26 18:25 | Gotcha | MEDIUM: VM Ubuntu bật lên "màn hình đen" sau khi host tắt đột ngột — KHÔNG phải hỏng filesystem, chỉ là Plymouth splash render tối màu + boot chậm. Chẩn đoán đúng làm từ HOST, không cần vào guest: `VBoxManage controlvm <vm> screenshotpng <file>` (thấy ngay logo Ubuntu = đang boot, chờ là xong) + `VBoxManage guestproperty enumerate <vm>` (có `GuestInfo/Net/0/V4/IP` = kernel+network đã sống; `OS/LoggedInUsers` 0→1 = boot xong) + `keyboardputscancode 01 81` để gửi Esc tắt splash. Kiểm `Logs\VBox.log`: không có `Guru Meditation`/`VERR_` ổ đĩa = đĩa ảo nguyên vẹn. Bẫy phụ: log đầy `supR3HardenedErrorV ... VERR_LDRVI_NOT_SIGNED ... uvh64.dll` (UltraViewer bị hardening chặn inject) là BÌNH THƯỜNG, đừng nhầm là nguyên nhân. Bài học: luôn screenshot trước khi kết luận VM hỏng — `fsck` chạy sai cách lên filesystem đang khỏe mới là thứ làm hỏng dữ liệu. Backup `.vdi` chỉ hợp lệ khi VM TẮT HẲN; bật VM giữa lúc copy → bản backup lệch kích thước, không restore được | `windows-tooling/virtualbox-chan-doan-vm-tu-host.md` |
| 118 | vietanh / App-Access-V2 (STEP-4.2 PLAN-migrate-avalonia) | 2026-07-26 | Gotcha | MEDIUM: Verify UIAutomation dialog Avalonia: owned window (`ShowDialog`) KHÔNG xuất hiện trong `RootElement.FindAll(TreeScope.Children)` → tưởng dialog không mở, debug nhầm hướng; kèm bẫy exception trong async void Click handler bị `InvokePattern.Invoke` nuốt im lặng (không crash, không log) — 2 triệu chứng giống hệt nhau. Fix: tìm hwnd bằng Win32 `EnumWindows` (ProcessId + GetWindowText==Title) rồi `AutomationElement.FromHandle`; debug handler bằng try/catch ghi ex.Message ra label UI. Bonus: `SelectionItemPattern.AddToSelection()` toggle được item KzCheckedListBox không cần mouse; mouse giả lập từ background process không tin cậy (SetForegroundWindow bị từ chối) | `avalonia/avalonia-uia-owned-dialog-not-desktop-child-async-void-invoke-swallow.md` |
| 117 | vietanh / DoorAlarmv3.Avalonia (STEP-3.5 PLAN-migrate-dooralarmv3-avalonia) | 2026-07-26 18:30 | Lỗi | HIGH: Thêm `ProjectReference Kztek.Cameras.Avalonia` làm vỡ build với 5 lỗi `CS0433` ở các file **ghi log** (`Program.cs`, `LoginViewModel`, `MainWindowViewModel`) — hoàn toàn không liên quan camera. Nguyên nhân: `Kztek.Object.MultyPlatform` (đi kèm thư viện camera) giữ NGUYÊN namespace + tên kiểu của `Kztek.Object.Entity` (`SystemUtils`, `SystemLog`, `EmSystemActionType`, `EmLogServiceType`) → compiler không chọn được. Fix: gom mọi điểm chạm vào **một lớp bọc `CoreLog`** đặt trong project chỉ tham chiếu Entity, thay vì `extern alias` phải khai báo ở mọi file. Bẫy kèm theo: đặt namespace `DoorAlarm.Core.Camera` (số ít) cạnh kiểu `Kztek.Cameras.Camera` gây `CS0118 'Camera' is a namespace but is used like a type` → phải dùng số nhiều `Cameras`. Dấu hiệu vàng: CS0433 nêu tên HAI assembly — đọc tên assembly, đừng đọc tên kiểu | `dotnet-general/kztek-object-entity-vs-multyplatform-cs0433-type-collision.md` |
| 120 | vietanh / App-Access-V2 (STEP-4.3 PLAN-migrate-avalonia) | 2026-07-26 | Gotcha | MEDIUM: Win32 `FindWindowW(null, title)` trả 0 với window Avalonia — CẢ top-level MainWindow, không chỉ owned dialog (mở rộng G005) — dù title khớp 100% từng codepoint (`Process.MainWindowTitle` đúng, dump codepoint xác nhận). Dễ kết luận nhầm "app không mở window". Fix: `EnumWindows` + `GetWindowTextW(CharSet.Unicode)` + `IsWindowVisible` so sánh chuỗi thủ công → hwnd đúng, `AutomationElement.FromHandle` dùng tiếp bình thường. Đừng debug encoding title — không phải nguyên nhân | `avalonia/avalonia-findwindoww-not-matching-avalonia-window-title.md` |
| 121 | vietanh / MathTool (Numina - Phase 0 xuất tài liệu) | 2026-07-26 18:50 | Gotcha | MEDIUM: `md_to_docx_kztek.py` báo `✗ PDF thất bại` với `[docx2pdf] Lỗi: Word.Application.Quit` hoặc RPC failed (-2147023170) cho 7/8 file — nhưng **cả 8 PDF đều đã được tạo đúng trên đĩa**. Nguyên nhân: docx2pdf điều khiển Word qua COM, Word ghi PDF xong rồi mới ném ngoại lệ ở bước đóng instance, nên thông báo lỗi không phản ánh kết quả thật. Retry từng file cho ra lỗi giả y hệt. Fix: `Get-ChildItem -Recurse -Include *.pdf | Select Name,Length,LastWriteTime` xác minh trên đĩa trước khi kết luận; KHÔNG cài LibreOffice/pypandoc, KHÔNG retry | `windows-tooling/docx2pdf-false-failure-pdf-still-created.md` |
| 122 | vietanh / MathTool-Numina (Phase 1 web app) | 2026-07-26 19:20 | Gotcha | HIGH: 2 bẫy React 19 + lazy-load. (1) `declare global { namespace JSX }` KHÔNG còn tác dụng cho custom element (`<math-field>`) — React 19 lấy JSX namespace từ chính module `react`, phải `declare module 'react' { namespace JSX { ... } }` + ghi kiểu tường minh cho handler; kèm bẫy `vite.config.ts` có khối `test` phải import `defineConfig` từ `vitest/config` chứ không phải `vite`. (2) Tráo ô nhập nhẹ sang component nặng ngay khi focus (để lazy-load 219KB gzip) → `import()` resolve GIỮA LÚC ĐANG GÕ, node cũ bị gỡ, phím rơi vào node chết → hệ thống báo 'chưa nhập gì', KHÔNG exception, dễ debug nhầm sang logic xử lý. Fix: chỉ tráo khi user chủ động bấm nút hoặc ngay lúc mount; focus chỉ prefetch, không đổi DOM | `react-web/react19-jsx-namespace-and-lazy-swap-swallows-keystrokes.md` |
| 123 | vietanh / App-Access-V2 (STEP-6.1 PLAN-migrate-avalonia) | 2026-07-26 21:15 | Gotcha | MEDIUM: Style Animation KeyFrame `RotateTransform.Angle` trên `Arc` KHÔNG chạy trên Avalonia 12.1 — build sạch, window mở bình thường nhưng spinner đứng hình hoàn toàn, silent (không exception/log); thử cả có lẫn không có `RenderTransform` local đều không xoay. Fix: xoay bằng `DispatcherTimer` 33ms cộng 10°/tick vào `RotateTransform.Angle` (code-behind), `Stop()` ở `Closed`. Bẫy verify kèm (G008): `Process.MainWindowHandle` trỏ window ACTIVE gần nhất → script pixel-diff dùng "loại trừ MainWindowHandle" bắt nhầm window gallery tĩnh → 2 lần "diff=0" giả; phải match window theo title qua `EnumWindows` + mở xem ảnh trước khi kết luận | `avalonia/avalonia-style-animation-rotatetransform-silent-not-running.md` |
| 123 | vietanh / MathTool-Numina (Phase 3) | 2026-07-26 21:40 | Gotcha | MEDIUM: `npx tsc --noEmit` báo SẠCH nhiều lần nhưng `npm run build` (`tsc -b`) vẫn FAIL. Nguyên nhân: scaffold Vite mới dùng project references — `tsconfig.json` gốc chỉ chứa `references` nên `tsc --noEmit` gần như không kiểm gì; `tsc -b` mới đọc `tsconfig.app.json` (nơi bật strict + `erasableSyntaxOnly`). Kèm bẫy `erasableSyntaxOnly` cấm **parameter property** (`constructor(private x)`) → TS1294, cũng cấm enum thường/namespace runtime. Fix: luôn kiểm bằng `npx tsc -b --noEmit` hoặc chạy thẳng `npm run build`; khai báo field rồi gán trong constructor | `react-web/tsc-noemit-vs-tsc-build-different-configs.md` |
| 124 | vietanh / iPGSv4 KIOSK HORIZONTAL (màn Cấu hình cảm ứng) | 2026-07-26 22:05 | Lỗi | HIGH: Bàn phím ảo làm kiểu LỚP PHỦ đáy che nội dung nhưng không chiếm chỗ → `ScrollViewer.Viewport` không co, phần form dưới bàn phím không bao giờ cuộn tới được, thanh nút Lưu/Huỷ bị che. Fix: bàn phím thành HÀNG RIÊNG trong Grid. Kèm: `Padding` của ScrollViewer không nới Extent (phải dùng `Margin` trên content), `BringIntoView()` phải Post sang lượt layout sau | `avalonia/avalonia-onscreen-keyboard-overlay-blocks-scroll.md` |
| 125 | vietanh / iPGSv4 KIOSK HORIZONTAL (MainViewModel timers) | 2026-07-26 22:20 | Lỗi | CRITICAL: `DispatcherTimer.Tick` chạy trên UI thread, handler gọi `SqlConnection.Open()` đồng bộ mỗi 10 giây → UI đóng băng từng đợt, cú chạm bị mất thất thường, Windows báo `not responding`, nhưng đồng hồ vẫn nhảy nên dễ tưởng UI còn khoẻ. Đã chẩn đoán sai 3 hướng (vùng chạm nhỏ / lớp phủ chặn hit-test / UIPI chặn input) trước khi `dotnet-stack report` chỉ đúng frame. Fix: `Task.Run` + `Dispatcher.UIThread.Post` để Start() lại timer | `avalonia/avalonia-dispatchertimer-sync-sql-freezes-ui.md` |
| 126 | vietanh / DoorAlarmv3 (migrate Avalonia — cau hinh vung camera) | 2026-07-26 23:10 | Gotcha | HIGH: Newtonsoft serialize `System.Drawing.Rectangle` qua `TypeConverter` thanh CHUOI `"1240, 880, 144, 192"` chu khong phai object `{"X":..}`. Port sang kieu tu viet (cross-platform) thi deserialize ra `default` (0,0,0,0) — KHONG exception, khong log, toan bo cau hinh cu bien mat. Fix: `JsonConverter<T>` giu dinh dang chuoi + probe round-trip tren CSDL that | `dotnet-general/newtonsoft-rectangle-serialized-as-string.md` |
| 127 | vietanh / DoorAlarmv3 (migrate Avalonia — UI pass) | 2026-07-26 23:15 | Gotcha | MEDIUM: `Style Selector="TextBlock"` toan cuc bat ca TextBlock do ControlTemplate sinh ra ben trong ContentPresenter cua Button → chu trong nut nen navy bi doi thanh mau chu toi, doc khong ra. Fix: dung selector co pham vi (`Border.section-header > TextBlock`, `.on-navy`) hoac sua ControlTheme, khong dung selector tran cho control nguyen thuy | `avalonia/avalonia-global-textblock-style-breaks-button-content.md` |
| vietanh / 6.RemoteControlTool | 2026-07-27 00:05 | linux-desktop/dconf-system-db-profile-required.md | Gotcha | dconf system-db bị bỏ qua nếu thiếu profile user; session cần re-login | Tạo /etc/dconf/profile/user (user-db:user + system-db:local) trước dconf update; reboot mới có hiệu lực; gesture 3 ngón không tắt được bằng gsettings |
| vietanh / iAccess App-Access-V2 (STEP-8.4 shell redesign) | 2026-07-27 00:20 | avalonia/avalonia-itemscollection-clear-fires-selectionchanged-minus-one.md | Gotcha | ListBox.Items.Clear() bắn SelectionChanged(-1) đồng bộ → wrapper forward gây crash ngầm lúc attach | Wrapper (KzSidebar) giữ+khôi phục SelectedIndex quanh rebuild; consumer guard index âm; set selection mặc định ở Window.Opened, không phải ctor |
| vietanh / IPGSUseCam | 2026-07-27 00:35 | avalonia/avalonia-control-overwrites-xaml-property.md | Lỗi | KzLabel ghi đè Foreground đặt trong XAML | `ApplyStyle()` gán trực tiếp `Foreground=...` (LocalValue) trong `OnAttachedToVisualTree` đè XAML mỗi lần re-attach; `IsSet()` không dò được vì DynamicResource chưa resolve; fix bằng `SetValue(..., BindingPriority.Style)` |
| vietanh / IPGSUseCam | 2026-07-27 00:35 | avalonia/avalonia-datagrid-auto-column-khong-do-lai.md | Gotcha | DataGrid cột Auto không đo lại khi thêm dòng mới | `Width="Auto"` chỉ đo tại lần materialize đầu, dòng mới nội dung dài hơn bị cắt; MinWidth không cứu được; fix bằng Width cố định hoặc `*` |
| vietanh / IPGSUseCam | 2026-07-27 01:10 | avalonia/avalonia-icon-emoji-khong-doc-duoc-o-co-nho.md | Gotcha | Icon emoji nát ở cỡ toolbar — không phải thiếu font | Pictograph nhiều chi tiết (✏,🗑) render đúng nhưng nát ở 12-15px; phóng to 4-5x mới thấy hình thật; fix bằng icon nét đơn giản hoặc bỏ icon giữ nhãn chữ |
| 128 | vietanh / DoorAlarmv3 (migrate Avalonia — man Bao cao) | 2026-07-27 01:15 | Loi | HIGH: `DatePicker.SelectedDate` la `DateTimeOffset?`, bind vao `DateTime` -> InvalidCastException hien DO ngay tren control, o ngay trong, ca man Bao cao khong dung duoc; build sach, log sach. Fix: property quy doi StartDate/EndDate. Bai hoc: loi binding Avalonia hien tren control chu khong nem exception -> self-test dung duoc cua so KHONG chung minh man hinh dung duoc | `avalonia/avalonia-datepicker-selecteddate-datetimeoffset.md` |
| 129 | vietanh / DoorAlarmv3 (canh bao camera AI) | 2026-07-27 01:20 | Loi | HIGH: `AnvPlayerService` dispose `boxedFrame` trong finally ngay sau khi ban `AIDetectAnalyzing`, kem comment "khong co subscriber nao (da kiem tra)". Subscriber dau tien xuat hien -> moi anh su kien 0 byte, `WriteableBitmap.Save()` khong nem loi. Fix phia app: sao chep pixel ngay dong dau handler, khong ghi duong dan ma vao CSDL, log dung 1 lan | `camera-integration/kztek-cameras-disposes-event-frame-after-invoke.md` |
| 130 | vietanh / App-Access-V2 (fix UX 8.9) | 2026-07-27 02:35 | Gotcha | TreeView co ScrollViewer noi bo — boc ScrollViewer ngoai Disabled khong chan duoc auto-scroll ngang khi select | `avalonia/avalonia-treeview-inner-scrollviewer-horizontal-autoscroll.md` — set `ScrollViewer.HorizontalScrollBarVisibility=Disabled` tren chinh TreeView |
| 131 | vietanh / App-Access-V2 (fix BUG-810) | 2026-07-27 04:20 | Gotcha | HIGH: Avalonia 12.1 drawn decorations -> `VisualRoot` cua control con la `TopLevelHost` KHONG phai `Window` -> guard `VisualRoot is Window` fail IM LANG, dialog khong mo, khong exception; kem bay `_ = ShowDialog(...)` vut Task nen fault khong toi global handler | `avalonia/avalonia-visualroot-toplevelhost-not-window.md` — dung `TopLevel.GetTopLevel(this)`, await ShowDialog + try/catch + log guard fail |
| 132 | vietanh / iPGSv4 (dong goi .deb Linux) | 2026-07-27 05:10 | Loi | Cai .deb xong KHONG co icon app: `.desktop` dung duong dan tuyet doi co typo (`.iIPGSUseCam` vs `.IPGSUseCam`) nen DE im lang bo qua; goi khong chua icon trong `/usr/share/icons/hicolor`, chi dua vao `postinst` goi `runuser install.sh` -> hong khi cai bang apt/gdebi (SUDO_USER rong) hoac may khong co runuser | `linux-desktop/deb-package-app-icon-missing.md` — dong goi icon vao hicolor + pixmaps, `.desktop` he thong dung `Icon=<ten>`, postinst chay gtk-update-icon-cache + update-desktop-database. Check nhanh: `dpkg -c file.deb \| grep icons/hicolor` |
| 133 | vietanh / iPGSv4 (UX luong thanh toan kiosk NGANG) | 2026-07-27 19:25 | Loi | HIGH: kich thuoc co dinh CAT noi dung IM LANG (build 0 Error, khong warning/exception): (1) `TextBlock Height="32"` + `FontSize="28"` -> cat chu tren/duoi, nhan ban 13 view do copy cung khuon; (2) root `Width="975"` ma noi dung can 1083 -> cat 108px ca cot nut thanh toan. Sai goc ca 2: doc nham so trong WinForms Designer (so 49 la le trai cua Label BEN TRONG panel, khong phai le cua control). Chi doc code KHONG phat hien duoc — phai chay app that + chup anh | `avalonia/avalonia-fixed-size-clips-content-silently.md` — luat: `Height >= FontSize*1.4` hoac dung `MinHeight`; cong tay le+cot+khe roi so voi Width root; lay khe/le that tu `Location` cua control con |
| 134 | vietanh / App-Access-V2 (RabbitMQ disconnect/reconnect warning) | 2026-07-28 00:00 | Gotcha | RabbitMQ.Client 6.8.1 (sync API, du an co y khong nang v7): event auto-recovery tren `IAutorecoveringConnection` ten la `RecoverySucceeded` (EventHandler<EventArgs>), KHONG phai `Recovery` nhu doan ban dau — verify bang reflect thang DLL trong `.nuget/packages` moi ra ten dung | `dotnet-general/rabbitmq-client-v6-recovery-event-name-recoverysucceeded.md` — cung fix chinh: them Disconnected/Reconnected event vao RabbitMqEventBus + KzToast warning o App.axaml.cs, loc Initiator==Application de khong bao gia khi tu Dispose() |
| 135 | vietanh / App-Access-V2 (AppShellWindow close-to-tray thong bao) | 2026-07-28 10:00 | Gotcha | `KzToastHost` la con cua chinh Window -> goi `Hide()` ngay sau `KzToast.Show()` trong `Closing` handler lam toast bien mat truoc khi user kip doc (minimize-to-tray khong thoat han) | `avalonia/avalonia-toast-child-window-hidden-on-hide.md` — fix: `DispatcherTimer` one-shot delay (1.2s) ngan hon thoi luong toast (3s) roi moi `Hide()` + bat TrayIcon; kem: them icon ky tu don gian (`↑`,`✕`) cho NativeMenuItem tray thay text thuan |
| 136 | trongtv / App-Access-V2 (PLAN-devicehost-kztek STEP-4.1) | 2026-07-28 | Gotcha | HIGH: Newtonsoft.Json throw JsonSerializationException khi deserialize class co >=2 constructor co tham so va khong co parameterless/[JsonConstructor] — du co public property setters; chi lo runtime khi data that chay qua (stub tra null khong trigger). G006 fix: JObject.Parse + map thu cong vao constructor 7-tham-so | `dotnet-general/newtonsoft-multi-ctor-no-default-deserialize-throw.md` |
| 137 | trongtv / App-Access-V2 (Loading 67 thiet bi nhung Main 0/0) | 2026-07-28 | Loi | HIGH: LoadingWindow bao thanh cong "67 thiet bi" (dem TONG `deviceResponse.Data.Count`) trong khi Main dung `AppState.BdkControllers` (loc theo Computer + loai purpose==0) rong -> KPI 0/0 im lang, khong loi do. Progress-text dem sai bien so voi bien man dich thuc su dung | `avalonia/avalonia-loading-progress-count-mismatch-vs-actual-usage.md` — fix: doi step.ResultText dung `AppState.BdkControllers.Count` sau loc, them log chan doan khi loc ra 0 de phan biet computer sai vs purpose==0 |
| 138 | ANHNV_2025 / parking-v8-app (UcConfirmOut/UcConfirmOutSmall btnEpass_Click - QR pending EPASS) | 2026-07-28 | Loi | Popup Form moi (`FrmEpassQRPending.ShowDialog(owner=maskedForm)`) mo de len dialog xac nhan ra xe (`dialogHost` - Form borderless rieng) bi che khuat IM LANG: khong exception, modal loop van chay dung (Invoke tu RabbitMQ callback van bom duoc), nhung khong thay cua so nao hien len - chi thay nut EPASS bi disable. Root cause: dialogHost giu z-order tren cung so voi maskedForm, Form con dung ShowDialog(maskedForm) khong TopMost bi che khuat truc quan. Fix tam thoi: dialogHost.Visible=false + frm.TopMost=true + ShowDialog() khong owner. Fix cuoi cung (theo yeu cau user): bo hoan toan Form rieng, viet lai thanh UserControl `UcEpassQRPending` dung pattern co san `UcSelectVouchers`/`MaskedUserControl` (khong Form doc lap, khong TopMost, tu track theo TargetControl) | `csharp-winforms/winforms-modal-child-hidden-behind-dialoghost.md` |
